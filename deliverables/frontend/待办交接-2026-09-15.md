# 前端待办交接清单（2026-09-15 02:30）

> 交接人：像素匠（frontend-developer）。本文档写给**下一个接手前端线的 agent**，
> 每一项都给出现状、依据（file:line / 命令）、验收标准。先读 §0 红线，再挑活。

---

## 0. 接手必读：红线与教训（违反会重蹈覆辙）

1. **前端三条硬约束已写入 `CLAUDE.md`**（`1f9b70a` 落盘）：
   ① 数据可溯源、禁合成兜底（失败显空态）；② view 禁裸 fetch（走 `utils/api.ts`）；
   ③ 缺失指标只能删或换 + 显式说明，**不许改口径硬凑**。
2. **`scripts/benchmark_results.json` 是 demo 合成数据**（`results.mode == "demo"`），
   已加体内 `_WARNING` 字段（`c6ac79b`）。**永远不要把它接进前端**。真产物 =
   `py-server/experiments/results/benchmark_YYYY-MM-DD.json`。
3. **多会话并发工作树**（今天同时活跃 4+ 个会话）。提交必须：
   `git diff --cached --name-only` 先查 index → `git commit -F <msgfile> -- <显式路径>` 局部提交 →
   `git show --stat HEAD` 复核。**提交信息一律写文件再 `-F`**（bash 反引号会吞字）。
4. **不要 `git checkout main` / 整树 checkout**：本环境会被 SIGTERM 中断，留下大量 `D` +
   `.git/index.lock`（今天实测一次，恢复后 0 丢失）。跨分支用 cherry-pick 指定 commit。
5. 大创中期（北极星）在 **`main`** 分支；本线工作在 **`career-literacy`**。
6. 归属判据：`var(--...)` 计数法**会误报**（新写 CSS 也用令牌）；必须读 diff，
   看"替换既有硬编码值"的 hunk 形态。

---

## 1. 已完成基线（4 个 commit）——不要重做

| commit | 内容 |
|---|---|
| `15c07e6` | `py-server/api/benchmark.py`（真产物接口，404 不回退）+ `src/composables/useBenchmark.ts`（唯一入口，零 fallback）+ `useBenchmark.spec.ts`（6 例）+ `tests/benchmark_evidence_gate.py`（R1/R2/R3，5 个变异体验证过） |
| `c6ac79b` | 路由注册（接口可达）+ `api.ts`（`upload`/`postBlob`/`postStream(signal)`）+ `studyStore.ts` 移除 `>=3` 丢数据门槛 + demo 产物 `_WARNING` |
| `1f9b70a` | BenchmarkView 四组硬编码清零、DashboardView 假状态灯/方差公式清零、App.vue 路由级 ErrorBoundary、6 处裸 fetch 收敛、CLAUDE.md 三条硬约束、`gate:evidence` 脚本 |
| `274613f` | CI `benchmark-evidence-gate` job |

**当前验证基线**：`vue-tsc` 0 错误 · `vitest` 77 passed · `vite build` 通过 · 门禁 R1/R2/R3 通过 ·
令牌零漂移。**改动后必须复跑这套再交差。**

---

## 2. 待办清单

### P1 · 契约层（主战场，约 4 人天）🔴

- **T1 建立 `src/types/`，从 OpenAPI 生成**（当前不存在；`any` 353 处且在涨）
  - 后端契约：`GET /openapi.json` 共 **216 条路径**（已实测）。建议把 schema 快照到
    `py-server/openapi.json` 入库，用 `openapi-typescript` 生成 `src/types/api.d.ts`。
  - 骨架参考：`src/composables/useBenchmark.ts` 的 `BenchmarkProvenance / RetrievalRow /
    QuestionRow / BenchmarkSummary`（已含真实字段名 `recall@5` / `mean_recall@5` 等）。
  - 验收：生成物入库 + `useBenchmark` 改为消费生成类型 + vue-tsc 0 + vitest 全绿。
  - ⚠️ 后端改字段会导致生成物 diff——把生成步骤挂进 CI（可复用 `benchmark-evidence-gate`
    job 的模式加一个 `types-drift` 检查）。
- **T2 消 `any`（353 处）**：从 benchmark 链路铺开到 stores → components → views。
  验收同上；**禁止**用 `any` 兜底让红变绿。

### P1.5 · 数据源治理 🟠

- **T3 处置 `scripts/benchmark_results.json`**：已有 `_WARNING` 体内标记，但文件名仍无标记。
  建议迁移到 `scripts/_demo/benchmark_results.json` 或改后缀 `.demo.json`，同步改
  `scripts/benchmark.py` 的默认输出路径与 `tests/benchmark_evidence_gate.py` R1 的说明注释。
- **T4 前端 benchmark 页面的时效性**：`api/benchmark.py` 挑选最新真产物（排除
  `reproduce`/`exp2`/`mode==demo`）。若后续跑出新日期产物，前端**无需改动**（provenance
  会自动带上新文件名/时间）——这是设计目标，验收时确认即可。

### P2 · 体验与性能（约 3 人天）🟡

- **T5 视图拆分到装配层（目标 ≤300 行/视图）**：现状 Top——`ResourceView.vue` 1324、
  `DashboardView.vue` 1210、`SkillPlatformView.vue` 932、`ChatView.vue` 838。
  模式：view 只做装配，逻辑下沉 composable，展示下沉组件。**每拆一个跑一次全套验证。**
- **T6 Dashboard Agent 卡的实时化前置条件**：后端**目前没有** agent 运行时接口
  （只有 `engine.py:196 /status`）。所以现状是静态职责卡（无状态灯）。**若后端将来加了
  运行时/SSE 接口**，才允许把职责卡升级为实时状态；在那之前**不许**重新画状态灯。
- **T7 加载/空态一致性**：`Skeleton.vue` / `EmptyState.vue` 已有，但覆盖不齐。目标：
  所有取数视图统一"loading→skeleton、失败→空态+重试、成功→provenance 可见"三态。
  参考 `BenchmarkView.vue` 的现成实现。
- **T8 a11y 收尾**（与 a11y 会话协同，他们有大量在途改动）：键盘导航、焦点管理、
  `prefers-reduced-motion`。**先看工作树里他们已做了什么，别重复劳动。**

### P3 · 演示可信度 🟢

- **T9 守住"如实说明"块**：`BenchmarkView.vue` 的如实说明（+7.14pp 召回 vs ≈213× 延迟、
  token −11.99% 未降反增）是**诚信设计，禁止删除或弱化**。该块全数据驱动，随后端选中最新
  真产物（`benchmark_2026-09-18.json`）自动更新，前端代码无需改。演示口径要背熟：
  FrugalRAG 赢在检索质量（recall/precision/MRR）与准确率（88.89% vs 76.67%，κ 0.8512/0.6884），
  代价是延迟（574.36 vs 2.70ms，≈213×）。⚠️ 旧口径（+10.7pp/20×/83.3%/κ0.776）已作废，
  见 §4 与证据索引 09-22 同步说明。
- **T10 同步到 main（大创线）**：用 `git cherry-pick 15c07e6 c6ac79b 1f9b70a 274613f`
  （在 main 上执行；冲突预期在 `ci.yml`/`package.json`/`CLAUDE.md`）。
  **不要 checkout main 工作树**（见 §0.4）。若嫌冲突烦，可只 cherry-pick
  `15c07e6`+`c6ac79b`（后端+契约，纯新增），消费方改造等大创线重做。

### 归属未清——**不要动，等对应会话**

- 工作树还有 **65 个已修改 + 30 个未跟踪**文件，属 a11y / design-system / ADR-007 等会话
  （`git status --porcelain` 自查）。接管前先跑 `git log --oneline -10` 看他们是否已提交。
- `deliverables/frontend/` 目录是**共享的**（已有卷二系列文件），新增文件用带日期的独立命名。

---

## 3. 验证命令速查

```bash
npm run gate:evidence        # 证据链门禁（R1/R2/R3）
npm run test                 # vitest
npm run type-check           # vue-tsc
npm run build-only           # vite build
python design-system/check_tokens.py   # 令牌漂移（CI 同款）
./py-server/.venv/Scripts/python.exe py-server/_verify_benchmark_api.py   # 接口自检（gitignored 本地工具）
```

## 4. 真数据速查（引用数字必须带口径）

> **⚠️ 2026-09-18 已重跑完整真基准**（`benchmark_2026-09-18.json`，当前发布权重，权重匹配 48/48）。
> 实验2 与 9/17 复现**逐位一致**；实验1 因 FrugalRAG 代码与 torch/numpy 版本演化而漂移（诚实实测）。
> 后端自动选中最新产物，前端页面（含"如实说明"块，全部数据驱动）已随之更新。

| 指标 | `benchmark_2026-09-18.json`（28 查询 / 30 题×3，seed 20260719） | 旧 `benchmark_2026-08-17.json` |
|---|---|---|
| 实验2 NeuralMixer 准确率 | **0.8889**（与 9/17 复现逐位一致） | 0.8333（Aug-15 权重） |
| 实验2 加权投票 / Δ | 0.7667 / **+12.2pp（+15.9%）** | 0.7667 / +6.7pp |
| Cohen's κ（NM↔真值 / 投票↔真值） | **0.8512 / 0.6884** | 0.776 / 0.688 |
| 实验1 Recall@5 Δ | **+7.1pp** | +10.7pp |
| 实验1 Precision@5 Δ | +15.7pp | +16.4pp |
| 实验1 Token | **−12%（FrugalRAG 反而多耗）** | −0.14% |
| 实验1 延迟 | 574.4 vs 2.7ms（**~213× 代价**） | 108.2 vs 5.4ms（20×） |

❌ 旧 demo 文案（+28pp、省 34% 噪声、矛盾检出 40 vs 0）**已作废**，别再引用。
⚠️ 交付件口径：大创材料已重基线到 0.8889（9/17 决策方案 A）；**若交付件引用了
"+10.7pp 召回 / 20× 延迟"，需同步到上表 09-18 口径或显式注明产物版本**（待办，见 T9）。

---

## 5. 任务状态更新（2026-09-18 像素匠续作）

- **T1 部分完成**：`src/types/api.generated.ts` 已存在（14k 行，含 benchmark 契约）；
  9/18 补齐后端 response_model + 契约快照 `py-server/openapi.json`（`8db94db`）+
  devDep `openapi-typescript` + `npm run types:generate`；`useBenchmark` 已挂显式
  `BenchmarkPayload` 契约（含 `toProvenance` 显式映射）。**遗留**：`api.generated.ts`
  的 benchmark 响应仍是开放签名——需在干净环境跑一次 `npm run types:generate`
  重生成（本机 node_modules 会被安全层隔离成 `*.DELETE.*`，传递依赖损坏，见 §0.7）。
- **T3 完成**：`scripts/benchmark.py` demo 输出已默认落 `scripts/_demo/*demo.json`；
  根目录旧 `scripts/benchmark_results.json` 仍有 `_WARNING` 体内标记。
- **T4 验证通过**：真跑 `py-server/experiments/benchmark.py` 产出 09-18 产物后，
  后端自动选中、前端页面与"如实说明"块自动更新——设计目标兑现，无需改前端。
- **T9 完成（2026-09-22 续作）**：证据页口径同步。`证据索引-2026-09-16.md/.html`、
  `证据一页图-2026-09-16.html` 三处检索基准段由 08-17 旧口径
  （+10.7pp/20×/83.3%/κ0.776/-0.14%）改为 09-18 真值
  （+7.14pp/≈213×/88.89%/κ0.8512/−11.99%），引用产物改 `benchmark_2026-09-18.json`
  （sha256 0c17657a4d85e50f），并加同步说明；产物清单补 09-18 行、计数 51→52。
  同步修前端物料 `frontend/overview.md`、`前端战略规划-2026-09-14.md` 真数据冲击表与 T9 描述。
  **仍残留（非本线/属其它会话）**：`engineering-assurance/metrics-integrity-audit-2026-08-29.md`（08-29 时点审计）、
  `平台现状摸底与发展谋划-2026-09-15.md`、`闽江申报材料归档-2026-09/benchmark_实验2_可复现性溯源-2026-09-17.md`
  （大创/main 线，由对应会话在 口径统一 时处理，勿越权改）。
- **T2 未动**（any 366 处）；T5~T8、T10 未动。
- **新增 §0.7 环境坑**：本机 node_modules 文件会被隔离/重命名（`*.DELETE.*` 后缀），
  依赖损坏时先查这个，别当成代码问题修。
