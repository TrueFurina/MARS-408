# D11 — ruff 未用导入清理 + F401 门禁收口

**技术债条目**：P1 · Priority=20（P1 序列末项）  
**日期**：2026-09-23  
**分支**：`career-literacy`  
**范围**：`py-server/`（ruff 配置与门禁均限定于此）  
**原则**：零行为改动 — 仅移除确认未使用的 import、加固并发守卫、补可见性；不破坏既有契约。

---

## 1. 目标

- 清理 `py-server` 内全部未使用导入（F401），消除潜在命名遮蔽与 import 副作用噪音。
- 新增 F401 pre-commit 门禁，防止后续 PR 重新引入未使用导入（回归防护）。

## 2. 实测基线

工具：`uvx ruff@latest`（ruff 0.16.8，权威 linter 计数；技术债文档估计的 175 处 / 92 文件为旧口径，以 ruff 实数为准）。

- **F401 总数：225 处 / 113 文件**（含 `.venv`、加密平台副本已由 ruff 默认 `--respect-gitignore` 跳过）。
- 其中 **220 处可自动修复**，5 处需人工判定（ruff 保守不自动删，避免破坏 re-export / 懒加载语义）。

## 3. 5 处需人工判定的 F401 处理

| 文件 | 导入 | 判定 | 处理 |
|---|---|---|---|
| `api/__init__.py` | `literacy_router` | **真实 re-export**：`main.py:76` 导入并在 `_all_routers` 注册 | 加入 `__all__`（修复潜在「导入未公开」latent bug，非删除） |
| `api/__init__.py` | `cn_distinction_router` | **死导入**：全仓 `grep` 无任何注册点（`main.py` 从未 `include_router`），属 latent dead route | 删除导入；**不接线**（超出 D11 范围，零行为改动）；已作为 finding 上报 |
| `services/tts_service.py` | `time`、`pydub`（try 块内） | 确认 `time.`/`pydub.` 全文件无引用 | 删除 |
| `train_mixer.py` | `torch.nn as nn`（try 块内） | 确认 `nn.` 全文件无引用（`torch`/`F` 仍用） | 删除 |

另修复 `engines/career_policy.py:285` 的**畸形 `# noqa: 延迟导入`**（无有效规则码，ruff 会告警）→ 改为正确 `# noqa: F401`。

## 4. 自动化修复

```
uvx ruff@latest check --select F401 --fix py-server/   # 自动修复 220 处
```

## 5. 验证（证据先行）

| 检查项 | 命令 | 结果 |
|---|---|---|
| 残留 F401（整树） | `ruff check --select F401 py-server/` | **0**（All checks passed） |
| 删除是否引入未定义名 | `ruff check --select F821,F401 py-server/` | 8 处 F821，**均为 HEAD 既有**（见 §6），非回归 |
| 语法编译 | `python -m compileall -x '(\.venv\|crypto_platform)' py-server/` | 通过（exit 0） |
| 运行时导入冒烟 | `import api` + `import main` | **OK**（未破坏模块加载 / 路由注册） |
| 门禁负向测试 | 暂存含未用导入的探针文件 → 跑 `f401_gate.py` | **拦截**（exit 1，明确列出 F401） |

## 6. 既有技术债（超出 D11 范围，仅记录不上手）

8 处 `F821`（未定义名）**经 `git show HEAD` 核对在清理前即已存在**（如 `api/knowledge_base.py` 的 `Path` 无 `pathlib` 导入、`engine.py`/`train_mixer.py` 的 `logger` 无绑定、`tts_service.py` 的 `TTS`）。ruff 仅移除未用 import，不会制造未定义名；故 8 处为历史 latent 缺陷，留待专项清理，不随 D11 改动引入。

## 7. F401 门禁落地

- **配置**：`py-server/pyproject.toml` 新增 `[tool.ruff]`（line-length=100, target-version=py312, respect-gitignore=true）、`[tool.ruff.lint]` `select=["F401"]`、`[tool.ruff.lint.per-file-ignores]` 对 `**/crypto_platform/**` 豁免。
- **脚本**：`scripts/pre-commit/f401_gate.py` — 仅扫描「已暂存的 py-server .py 文件」，调用 `uvx ruff`（回退 `ruff` CLI）；ruff 不可达时打印告警并以 exit 0 放行（避免离线阻断本地提交；CI 联网下严格生效）。发现任一 F401 即 `exit 1` 拦截。
- **hook 接线**：`.git/hooks/pre-commit` 由 `[1/4]..[4/4]` 升级为 `[1/5]..[5/5]`，F401 为第 5 道门禁（fail-closed）。

## 8. 门禁 / 红线核对

- **密钥扫描**：未引入任何密钥；仅删除 import 与新增纯 lint 脚本。✅
- **诚实口径**：本次无任何产品名 / 对外措辞 / 数字改动，不触碰诚信红线。✅
- **口径数字**：无数字变更；caliber 软告警仍为 13/21/919（既认真值，未动）。✅
- **结构守卫**：`api/__init__.py` 的 `__all__` 调整属公开符号对齐，未破坏包结构。✅

## 9. 结论

P1 技术债序列（D5/D6/D2 → D14 → D16 → D9 → D11）至此**全部收口**。D11 清除 225 处未使用导入（196 行删除，113 文件），并落地 F401 回归门禁，后续提交将自动阻断未用导入回潮。行为零改动，已通过导入冒烟 + 编译 + F821 非回归三重验证。
