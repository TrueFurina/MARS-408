# 408 知识图谱导入 — 验证报告（kg408）

> 本文件是**正式交付件**（供中期材料引用）。所有数字均为**实测值**，采集命令见 §6，不作任何手写推断。
> 采集时间：2026-09-26（worktree `E:/Program/MARL/mars-408-kg`，分支 `feat/kg408`，HEAD `d21cd72`，**未 commit**）。
> 采集环境（**双解释器**）：
> - **脚本闸门（build / bind / verify）**：`C:/Users/Lenovo/.workbuddy/binaries/python/versions/3.13.12/python.exe`（脚本纯标准库即可；未使用 `uv run`）。
> - **TESTS 闸门（pytest）**：须用**带 pytest 的解释器**（如 `/d/miniconda3_new/python.exe`，或任一含 pytest 的 venv）；上述 workbuddy 的 `3.13.12/python.exe` **无 pytest（实跑报 `No module named pytest`），不能用来跑该步**。

---

## 1. 三道闸门结果（末次干净复跑）

| 闸门 | 命令 | 结果 |
|---|---|---|
| 1. BUILD | `python scripts/build_kg408.py` | `exit=0`，连跑两次**逐字节一致** |
| 2. BIND | `python scripts/bind_kb_to_kg408.py` | `exit=0` |
| 3. VERIFY | `python scripts/verify_kg408.py`（**无参全量**） | `exit=0` → `[verify] ✅ 全部通过` |
| 4. TESTS | `pytest tests/test_kg408.py` | **`43 passed`**，exit=0（JUnit XML：`tests=43 failures=0 errors=0 skipped=0`） |

VERIFY 无参全量现含四段关卡（任一失败 exit 1）：

```
[verify] 关卡 1/3 结构自检 …
[verify] 关卡 2/3 可复现性（重跑比对）…
[verify] 关卡 2b/3 可复现性（KB 覆盖率产物重跑比对）…
[verify] 关卡 3/3 文档数字 == 重算真值（kg408.json / KB binder）…
[verify] 真值：节点 136（源骨架 135 + 人工补充 1） / 边 18（先修 14 + 跨科 4） / 未解析 17 / 依赖别名表 16
[verify] KB 覆盖率真值：考点级 33.96%（36/106） / 章级 96.15% / chunk 绑定 46
[verify] ✅ 全部通过
```

### 1.1 BUILD 连跑两次的 sha256（逐字节可复现）

| 产物 | sha256（两次相同） |
|---|---|
| `py-server/data/kg408/kg408.json` | `9b29202299bb4024d171e4ad283bada305eff844477c211f080e975a3920940e` |
| `py-server/data/kg408/kg408_stats.json` | `adcb0a1d2b95d37f1386bd061a06a0e55ec97f0410c4fad04943a2de70718d94` |
| `py-server/data/kg408/unresolved.json` | `403992a1b22fa29ff1f7e1d0dd715f935ad5d38f20cf30ded6ba9c1326fbdea1` |
| `deliverables/408-kg/kg408-stats.md` | `551ac4b99bbf98a78df4c20de3464f456448d2439c090916b9b8d758a591b522` |

> 跨平台说明：全部产物**刻意写 LF**（`newline=""`，见 `build_kg408.py:817 _write_text_lf`、`bind_kb_to_kg408.py:75`）。
> 修复前 Windows 下 `Path.write_text` 会把 `\n` 翻成 `\r\n`，导致「逐字节可复现」只在 Windows 内成立、Linux CI 上失效。
> 现有回归测试 `tests/test_kg408.py:711 test_generated_artifacts_use_lf_line_endings` 守住这条。

---

## 2. 关键结论数字（如实口径）

| 项 | 值 |
|---|---|
| 节点合计 | **136 = 源骨架 135 + 人工补充 1**（两数**分开列，禁止合并**） |
| 层级 | 科目 4 / 章 26 / 考点 106（源 105 + 人工 1） |
| 边 | 18 = 先修 14 + 跨科关联 4 |
| 未解析（如实挂起） | **17**（`absent_in_skeleton` 11 / `chapter_level_only` 5 / `ambiguous_multi_match` 1） |
| 依赖人工别名表的边 | **16/18**（resolution：alias 16 / exact 2） |
| 跨科边权重 | **全 null（18/18）**，无 0.5/1.0 伪造 |
| 先修声明 | 源声明 23 条 → 已解析 14 条，成功率 **60.9%** |
| KB 对齐 | chunk 1892（考点级 748）／章级绑定 1684（89.01%）／章覆盖率 96.15%／**考点级覆盖率 33.96%（36/106）**／考点级绑定 46（subtopic_exact 43 + alias 3） |
| 模糊匹配 | 全程 **未使用任何相似度阈值**，绑不上记 `kp_id=null` |

---

## 3. 防伪证变异测试（4 个场景，**无参 verify 必须 exit 1**）

| 场景 | 变异内容 | VERIFY_EXIT | 命中的关键断言 |
|---|---|---|---|
| 基准 | 干净态 | **0** | — |
| **GAP-1A** | 改 `deliverables/408-kg/kb-alignment.md` 的 `33.96` → `99.99` | **1** | ①重跑后 md 逐字节不一致；②md 数字与重算真值不符（`33.96`）；③正文声明数字不符 |
| **GAP-1B** | 改 `kp_chunk_coverage.json` 的 `kp_coverage_pct` `33.96` → `99.99` | **1** | ①`coverage[kp_level.kp_coverage_pct]=99.99` ≠ 重算 `33.96`；②重跑后逐字节不一致 |
| **GAP-2-P1** | 改 `kg408_alias.json` 中「调度」目标 → `CO/中央处理器/中断`，**并按 `provenance.command` 重跑 build**（rc=0） | **1** | ①`当前 kg408_alias.json 表 sha ≠ 冻结清单 sha`；②KB 产物逐字节不一致 |
| **GAP-2-P2** | 往 `kg408_manual_nodes.json` 塞伪造节点 `408:OS:CH04:KP99`，**并重跑 build**（rc=0） | **1** | ①`当前 kg408_manual_nodes.json 表 sha ≠ 冻结清单 sha`；②覆盖率真值漂移（`kp_total` 106→107）；③三个 KB 产物逐字节不一致 |

> GAP-2 的关键点：**sha 是 build 当场从当前文件算出来的**，所以「改表 + 重跑」会让 `provenance` 锚点自动跟上。
> 因此新增**独立冻结清单**（只允许人工更新）作为第二锚点，见 §4。

### 3.1 变异测试后工作区还原判定

```
RESTORE_DRIFT: 无（全部逐字节还原）
还原后 VERIFY_EXIT=0
```

---

## 4. 冻结基线清单（防「改表 + 重跑」）

文件：`py-server/data/kg408/kg408_inputs_frozen.json`（sha256 `e13ba080bf4d48b0ac4ef84eca0cdb42671a28731937a52af90d61a0a138740b`）。
**该文件不得由任何生成脚本改写**（build/bind 均不写它），只允许人工更新，且更新前必须完成学科负责人复核。

| 输入表 | 冻结 sha256 | `review_status`（**如实**） |
|---|---|---|
| `kg408_alias.json` | `d97bcdfe5e12c0ab8a225f3a437e90edec452d40e13235fa5f7cbfea78e3162f` | `pending_discipline_owner_review`（25 条映射中**仅 2 条**经 2026-09-25 裁决：『调度』改判 + 新增『文件-磁盘调度』） |
| `kg408_chapter_map.json` | `f2a9e2a55c841247bc756f63e8c1d857aca65ab15070970109517b16c2aaf6ec` | `pending_discipline_owner_review` |
| `kg408_manual_nodes.json` | `767effe393b1d6b170a64ffd43ccfc8b17ecc8843055af923eb19fe84c8e2dc0` | `approved_by_discipline_owner`（2026-09-25 裁决） |

> 诚信声明：清单是**冻结基线 + 漂移检测**，**不是**「全表已批准」。上表 `review_status` 如实反映「只有人工补充节点表获批准，alias/chapter_map 整体仍待复核」。
> 回归测试 `tests/test_kg408.py:827 test_frozen_manifest_review_status_is_truthful` 强制这一点，禁止把未复核的表虚标为已批准。

---

## 5. 最终产物 sha256（未 commit 状态下的工作区真值）

### 5.1 生成产物（7 个）

| # | 文件 | sha256 |
|---|---|---|
| 1 | `py-server/data/kg408/kg408.json` | `9b29202299bb4024d171e4ad283bada305eff844477c211f080e975a3920940e` |
| 2 | `py-server/data/kg408/kg408_stats.json` | `adcb0a1d2b95d37f1386bd061a06a0e55ec97f0410c4fad04943a2de70718d94` |
| 3 | `py-server/data/kg408/unresolved.json` | `403992a1b22fa29ff1f7e1d0dd715f935ad5d38f20cf30ded6ba9c1326fbdea1` |
| 4 | `deliverables/408-kg/kg408-stats.md` | `551ac4b99bbf98a78df4c20de3464f456448d2439c090916b9b8d758a591b522` |
| 5 | `py-server/data/kg408/kp_chunk_coverage.json` | `1dfc1321bd0ceb656dc5b3481d2c3e681f88978dfbf1eb021f6829baf8b6273f` |
| 6 | `py-server/data/kg408/kp_chunk_binding.json` | `8244b475d7147d30f0e9f0d12fcb08b24feef11e414611fe0f2926c6bf4e3bac` |
| 7 | `deliverables/408-kg/kb-alignment.md` | `475d7087607a5e0e762391db406ad02bafb2260c65b852d742d52f4d75d0365e` |

### 5.2 冻结清单

| 文件 | sha256 |
|---|---|
| `py-server/data/kg408/kg408_inputs_frozen.json` | `e13ba080bf4d48b0ac4ef84eca0cdb42671a28731937a52af90d61a0a138740b` |

### 5.3 输入表（人工维护，受冻结保护）

| 文件 | sha256 |
|---|---|
| `kg408_alias.json` | `d97bcdfe5e12c0ab8a225f3a437e90edec452d40e13235fa5f7cbfea78e3162f` |
| `kg408_chapter_map.json` | `f2a9e2a55c841247bc756f63e8c1d857aca65ab15070970109517b16c2aaf6ec` |
| `kg408_manual_nodes.json` | `767effe393b1d6b170a64ffd43ccfc8b17ecc8843055af923eb19fe84c8e2dc0` |

> 全部生成产物的 CRLF 计数均为 **0**（纯 LF），保证跨平台逐字节复现。

---

## 6. 复现命令（照抄即可）

```bash
cd py-server
PY="C:/Users/Lenovo/.workbuddy/binaries/python/versions/3.13.12/python.exe"   # 脚本闸门：纯标准库解释器即可
PYT="/d/miniconda3_new/python.exe"   # TESTS 闸门：必须是带 pytest 的解释器（workbuddy 的 3.13.12 无 pytest）

# 1. 生成（连跑两次应为同一 sha256）
"$PY" scripts/build_kg408.py
"$PY" scripts/build_kg408.py

# 2. KB 覆盖率产物
"$PY" scripts/bind_kb_to_kg408.py

# 3. 防伪证闸门（**无参 = 全量**，唯一可信模式）
"$PY" scripts/verify_kg408.py ; echo "exit=$?"

# 4. 回归测试（**换用带 pytest 的解释器**；上方 $PY 无 pytest）
"$PYT" -m pytest tests/test_kg408.py -q ; echo "exit=$?"
```

⚠️ 弱模式警告（已写入产物头部）：`--check-doc` 只跑文档数字关；`--no-repro` 跳过重跑比对。
二者都**拦不住** `kg408_stats.json` 被篡改，也拦不住 `kg408.json` 内容被改。**防伪证必须跑无参全量。**

---

## 7. 本轮修复锚点（file:line）

| GAP | 修复位置 |
|---|---|
| **GAP-1**（覆盖率产物无闸门） | `verify_kg408.py:118` `FROZEN_MANIFEST_NAME`；`:528` `KB_DOC_ROW_CHECKS`；`:555` `compute_kb_truth`；`:580` `assert_kb_coverage_file_is_derived`；`:618` `assert_kb_doc_matches_truth`；`:713` `rebuild_and_compare_kb`（关卡 2b）；`bind_kb_to_kg408.py:386` `--write-dir`（读真源、写临时目录） |
| **GAP-2**（改表 + 重跑自洽） | `verify_kg408.py:394` `assert_provenance_anchors`（新增冻结比对，报错文案见 `:449`）；`:457` `_load_frozen_manifest`；清单文件 `data/kg408/kg408_inputs_frozen.json` |
| **GAP-3.1**（弱模式误导） | `build_kg408.py` `render_stats_md` 头部；`bind_kb_to_kg408.py:289` `render_alignment_md` 头部 |
| **GAP-3.2**（成功率分子分母单位不一致） | `build_kg408.py:514/718/795` 新增 `source_prereq_declarations_resolved`；`services/kg408.py:530/535` 分子改用同单位的已解析声明数 |
| **GAP-3.3**（源骨架字节数未被断言） | `verify_kg408.py` `DOC_TRUTH_FIELDS` 增 `("源骨架字节数", "provenance.source_bytes")`，文档关用「真值 + provenance」视图 |
| **额外（QA 发现）** `--out-dir` 误写交付物 | `build_kg408.py:1089` `resolve_doc_out`：`--out-dir` 非默认数据目录时，`--doc-out` 跟随 `out_dir`；自测确认真实 `deliverables/408-kg/kg408-stats.md` 的 sha 与 mtime **均未变** |
| **额外（跨平台可复现）** | `build_kg408.py:806/817`、`bind_kb_to_kg408.py:75` 全部写 LF；回归 `tests/test_kg408.py:711` |

### 7.1 新增回归测试

| 测试 | 作用 |
|---|---|
| `tests/test_kg408.py:659` `_snapshot_and_restore_artifacts` | 模块级 autouse 快照/还原（含**输入表**），单测失败也不留篡改态 |
| `:711` `test_generated_artifacts_use_lf_line_endings` | 产物必须 LF（跨平台逐字节复现） |
| `:735` `test_kp_coverage_exact_numbers` | 显式断言 33.96 / 36 / 106 / 46 / 89.01 / 96.15 / 1684 |
| `:763` `test_kb_alignment_doc_matches_coverage_numbers` | 对外人读 md 的数字与 coverage 一致（含禁止残留旧分母 105） |
| `:777` `test_verify_catches_kb_alignment_md_tampering` | GAP-1A 变异 → verify exit 1 |
| `:789` `test_verify_catches_coverage_json_tampering` | GAP-1B 变异 → verify exit 1 |
| `:806` `test_frozen_manifest_pins_input_tables` | 输入表 sha == 冻结基线 |
| `:827` `test_frozen_manifest_review_status_is_truthful` | 禁止虚标「全表已批准」 |
| `:849` `test_verify_catches_input_table_drift_despite_rebuild` | GAP-2-P1：改表 + 重跑 → verify exit 1 |
| `:899` `test_verify_catches_forged_manual_node_despite_rebuild` | GAP-2-P2：伪造人工节点 + 重跑 → verify exit 1 |
