# KB chunk ↔ 408 考点 对齐覆盖率报告

> 由 `scripts/bind_kb_to_kg408.py` 自动生成。覆盖率偏低**照写不修饰**——本文件的价值在于可回溯，不在于好看。
>
> **防伪证**：本文件数字由 `scripts/verify_kg408.py`（**无参 = 全量**）逐项断言；`kp_chunk_coverage.json` 与 `kp_chunk_binding.json` 亦参与重跑逐字节比对。任一数字被改 → exit 1。
> 本文件刻意**不含墙钟时刻**（仅打印到 stdout），以保证逐字节可复现。

## 1. 总体

| 指标 | 数值 |
|---|---|
| KB chunk 总数 | 1892 |
| 其中 knowledge_point | 748 |
| 其中 knowledge_variant | 1144 |
| 章级已绑定 | 1684（89.01%） |
| 仅科目级绑定（无章） | 163 |
| 章级未绑定 | 45 |
| 骨架章总数 | 26 |
| 有 chunk 命中的章数 | 25 |

## 2. 覆盖率

| 口径 | 覆盖率 |
|---|---|
| 章级覆盖率（有 chunk 的章 / 骨架章） | 96.15% |
| 考点级覆盖率（有 chunk 的考点 / 106 考点） | **33.96%** |
| 考点级 chunk 绑定率（已绑定 / 可绑定 748 条） | 6.15% |

**考点级覆盖率 = 33.96%（36/106）**。这是**如实值**：骨架考点名与 KB 的 `sub_topic` 是两套各自独立命名的体系，本阶段只做全等匹配，不做语义/模糊对齐，因此覆盖率天然偏低。提升路径只有两条合法：① 人工补 alias 表；② 由错题共现学习。两者都不得伪造。

## 3. 考点级匹配方式分布

| match_method | 条数 | confidence |
|---|---|---|
| alias | 3 | 0.8 |
| subtopic_exact | 43 | 1.0 |
| **合计** | 46 | |

- knowledge_variant（1144 条）按设计**不参与考点级强制绑定**，仅参与章级统计。
- 可绑定但未能绑定的 chunk：702 条，记为 `kp_id=null`。

## 4. 匹配口径（诚信声明）

- 允许的 match_method：`chapter_exact` / `subject_exact` / `subtopic_exact` / `alias`。
- **未使用任何模糊相似度阈值**。
- 绑不上的 chunk **不丢弃、不伪造**，一律 `kp_id=null` 并保留原因。
