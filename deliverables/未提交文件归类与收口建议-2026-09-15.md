# 未提交文件归类与收口建议（2026-09-15 16:58）

> 配套《平台现状摸底与发展谋划》。共 **91 个未提交条目**，均来自并发会话的 WIP。
> 本清单目的：让各会话 owner 能**精准局部提交 / 协调**，避免误 `git checkout` 或互相覆盖导致丢失。
> ⚠️ 像素匠**不代为提交**（遵守 `待办交接-2026-09-15.md` §0 纪律），仅提供归类与命令模板。

---

## ⚠️ 头号风险：67 个 .vue 是「交叉编辑」，不能按目录盲拆

`src/` 下 67 个 `.vue`（含 41 个 view 中绝大部分 + 组件）同时被**两个会话**改动：
- **design-system 会话**：新增 UI 原语 `GlassCard/Stack/SectionHeader/TagList/EngineSection.vue` + token 化 codemod（`scripts/codemod_tokens.py`、`check_vue_tokens.py`、`vue_token_baseline.json`）+ 把视图迁到新令牌/新原语。
- **a11y 会话**：给同一批视图加无障碍属性（`scripts/_a11y_check.py`）。

→ 这两个会话的改动**落在同一批文件里**。若任一方整目录 `git add src/ && git commit`，会把另一方未完成的半成品一并提交或冲突。**必须先协调出提交顺序**：
1. **design-system 先提交**（它是基础：新原语 + token 基线，a11y 的无障碍属性应叠在 token 化之后的代码上）；
2. a11y 在 design-system 提交后 `git pull/rebase`，再提交自己的 a11y 属性增量。

---

## A 组 · design-system 会话（建议先提交）

| 文件 | 性质 |
|---|---|
| `design-system/check_vue_tokens.py` | 新增：视图裸色值漂移检查 |
| `design-system/vue_token_baseline.json` | 新增：令牌基线快照 |
| `src/components/GlassCard.vue` `Stack.vue` `SectionHeader.vue` `TagList.vue` `EngineSection.vue` | 新增 UI 原语 |
| `src/components/__tests__/` | 新增：原语单测 |
| `scripts/codemod_tokens.py` `scripts/_ui_audit.py` `scripts/_ui_audit2.py` | 新增：token codemod / UI 审计脚本 |
| `src/views/DesignSystemView.vue`（及被 token 化的其余 67 视图） | 视图迁到新令牌/原语 |

**命令模板**（design-system owner 执行，仅加自己文件）：
```bash
git add design-system/ src/components/GlassCard.vue src/components/Stack.vue \
  src/components/SectionHeader.vue src/components/TagList.vue src/components/EngineSection.vue \
  src/components/__tests__/ scripts/codemod_tokens.py scripts/_ui_audit.py scripts/_ui_audit2.py
# 67 视图中"纯 token 化"的部分——需 owner 用 git diff 逐文件确认归属后再加
git commit -F design-system_commit_msg.txt   # 消息写文件，防 bash 反引号吞字
```

## B 组 · a11y 会话（design-system 提交后 rebase 再提交）

| 文件 | 性质 |
|---|---|
| `scripts/_a11y_check.py` | 新增：无障碍检查脚本 |
| `src/views/*.vue` + `src/components/*.vue` 中的无障碍属性增量 | 与 A 组**同文件**，须 rebase 后取差 |

**命令模板**（a11y owner 执行）：
```bash
git fetch origin && git rebase origin/career-literacy   # 先吃到 design-system 的提交
# 用 git diff 复核：只 add 自己加的无障碍属性 hunk（必要时 git add -p 交互选 hunk）
git commit -F a11y_commit_msg.txt
```

## C 组 · docs / ADR / engineering-assurance 会话

| 文件 | 性质 |
|---|---|
| `docs/MARS-408考研个性化学习系统_v1.0.md` `docs/MARS-408考研系统架构.md` `docs/README-考研系统项目说明文档.md` `docs/应用密码学融合培养方案_v1.0.md` `docs/职业素养对抗实训改造方案.md` `docs/考研系统12维度能力画像与构念效度_v2.1.md` `docs/证据链对评审可信度的价值...md` | 新增/改：MARS-408 / 芒得很职 / 教改方案文档（**含大量"真口径"数字，须核对 provenance 再提交**） |
| `diagnostics/复盘诊断报告-2026-09-15.md` | 新增：诊断 |
| `deliverables/engineering-assurance/INCIDENT-parallel-clone-clobber-2026-09-15.md` `TODO-pending-fixes-2026-09-15.md` `deep-audit-career-literacy-2026-09-15.md` `eng-assurance-deep-audit-career-2026-09-14.md` `fix-wave-career-remediation-2026-09-15.md` | 新增：工程保障审计/事故/待修 |
| `deliverables/frontend/` | 共享目录（已有卷二系列，新增文件用带日期独立命名） |

**命令模板**：
```bash
git add docs/ diagnostics/ deliverables/engineering-assurance/
git commit -F docs_commit_msg.txt
```

## D 组 · 后端会话

| 文件 | 性质 |
|---|---|
| `py-server/config.py` | 改：配置 |
| `py-server/tests/test_career_policy_baseline.py` `test_review_single_source.py` | 改：测试（注意 CLAUDE.md 单测写者纪律） |
| `Dockerfile` | 改：构建 |

**命令模板**：
```bash
git add py-server/config.py py-server/tests/test_career_policy_baseline.py \
  py-server/tests/test_review_single_source.py Dockerfile
git commit -F backend_commit_msg.txt
```

---

## 收口后的全局校验（每个 owner 提交完跑一遍）

```bash
npm run type-check && npm run test && npm run gate:evidence
# 预期：vue-tsc 0 错误 / vitest 77+ passed / 证据链 R1-R2-R3 过
# 全绿后再 git push origin career-literacy
```

## 若会话已废弃的兜底方案（D2🅑）

不代提交、不删：用 `git stash push -m "<owner>-WIP-2026-09-15" -- <paths>` 按组归档到 stash，
保留全部内容、不混入主线；待你确认后 `git stash pop` 恢复。
