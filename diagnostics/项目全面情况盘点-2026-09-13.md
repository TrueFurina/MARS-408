# 项目全面情况盘点（2026-09-13 续 · 落盘反查版）

> 范围：以仓库真实状态（git / 磁盘 / 运行配置 / README / CLAUDE.md / 今日诊断报告）为准，不采信文档自述。
> 立场：本盘点只陈述事实与风险，未改动任何受检文档（遵守只读+仅追加红线）。

---

## 〇、一句话总结

仓库 **study-help-pro**（GitHub: MARS-408）是**双分支双身份**工程：
`main` = **MARS-408**（408 考研多智能体系统，国家级大创结题）；
`career-literacy` = **芒得很职**（职业素养对抗实训，三创赛）。
当前分支 `career-literacy` 存在一个**未完成的 rebase 悬挂状态（高风险）**，须先决断；
工程底座真实可信，但**中期报告有 1 处红线级事实错误（X2 通道）**待订正；
**E5 向量模型今日已恢复完整**，推翻了今日诊断报告 "E5 缺失" 的判断。

---

## 一、仓库身份与双分支

| 分支 | 项目身份 | 赛事/阶段 | 当前 |
|---|---|---|---|
| `main` | MARS-408 · 408 考研个性化学习系统 | 国家级大创结题 | 大创中期 Nov 2026 真版 |
| `career-literacy` | 芒得很职 · 职业素养对抗实训 | 第十六届三创赛 | **当前所在分支，开发中** |

- 双分支通过 `main → career-literacy` 单向同步共享底座；芒得很职新增代码全部 `career_*` 前缀，零侵入 408 代码。
- 远程 `origin/career-literacy` 存在；当前本地 `career-literacy` 领先 origin **1 个 commit**（`3f4b66a` MAPPO 服务器训练脚本）。

---

## 二、⚠️ 工作树关键状态（高风险，须先决断）

**悬挂的 rebase：** `career-literacy` 正在对 `b72d9f5` 做**交互式 rebase**，状态卡在 "editing a commit"：
- `No commands done`，仍有 **2 个待 pick 的 commit 未应用**：
  - `2f5bab7` feat(frontend): 四支柱大改 — 视觉重做/动效/IA 导航重构/响应式适配
  - `e5d6ddb` docs: 前端大改配套 showcase + 设计系统/IA 文档与交付报告
- 无未合并文件（无冲突），但 rebase 未完成会阻塞后续任何干净 git 操作。
- `.git/rebase-merge/` 存在 `interactive`、`git-rebase-todo`、`onto=b72d9f5`。

**未跟踪文件：** `_midterm_text.txt`（中期报告文本摘录）、`diagnostics/深度质检与评价-2026-09-13.md`（今日质检）。

> **Leader 裁决建议（二选一，我不擅自执行）：**
> - **A. 继续 rebase**：`git rebase --continue`（需确认 2 个 pending commit 应用无误、无冲突）；
> - **B. 中止 rebase**：`git rebase --abort`（回到 rebase 前干净状态，pending commit 保留在逻辑位置）。
> 推荐先 `git rebase --continue` 收口，因无冲突且只是把既定 commit 重放一遍；若担心引入冲突则先 abort。

---

## 三、MARS-408（main 分支）能力兑现现状

依据 `README §〇` 2026-09-02/12 实测核对（三档标注，对外口径以此为准）：

| 能力 | 状态 | 实测依据 |
|---|---|---|
| 10 节点 LangGraph 流水线 | ✅ 真 | 全节点真跑，SSE 至 `[DONE]` |
| 后端服务 | ✅ 真 | 冷启动 936ms，加载 2113 条向量 |
| 真实对话生成 | ✅ 真 | POST /api/chat/stream → 200 / 5.4s / 有效 LLM 内容 |
| 检索增强效果 | ✅ 真 | Recall@5 +15.8% / MRR +16.0%（benchmark 2026-08-17） |
| 三级降级容灾 | ✅ 真 | Redis→内存 / PG→SQLite / Milvus→InMemory 逐级回退 |
| 前端页面 | ✅ 真 | 38 个 views / 70 个 .vue |
| 共识引擎 | ✅ 规则原型 + 三评审门禁 | M2 critic 结构化 + 证据门禁 + 置信度 |
| Triage 分级路由 | ✅ 真 | 零 LLM 成本分类器 + low 短路 |
| MAPPO 教学策略层 | ✅ 已训练（合成环境） | 13 节点冒烟通过；checkpoint `models/mappo_policy.pt` |
| MARL 算法对比 | ✅ 真数据 | IQL/VDN/QMIX/MAPPO，MAPPO 最优最稳 |
| 向量检索 | ⚠️ 降级运行 | 见第五节：E5 模型今日已恢复，需验证真启用 |
| LLM 通道 | ⚠️ 与文档有别 | 实际 **DeepSeek 主 + generalv3.5 兜底**；X2 未授权（11200） |

工程指标：后端 414 个 py / 约 10.2 万行；前端 93 文件 / 约 2.8 万行；616 项测试定义（Windows CI 198 通过，env 级 SIGSEGV 阻断其余，以 Linux CI 为准）。

---

## 四、今日（2026-09-13）深度质检报告结论

受检对象：中期检查报告书（references/附件1）。方法：全部落盘反查，不采信自述。

**已核实真实（可保留作卖点）：**
- NeuralMixer **48/48 权重真训**（`mixer_before_after_2026-09-03.json`）；
- KB `netlearn_kb.json` **2122 条 embedding，全 L2=1.0，0 零向量**（存储侧）；
- 检索增强 relative +15.8%/+16.0% 与中期绝对 +10.7pp/+9.6pp **同源一致**；
- MAPPO/MARL 算法对比、**career LLM 冒烟 overall=4.5**、**KG 0 节点诚实自纠（613→0）** 均真。

**发现的问题（须订正）：**
- 🔴 **P0（红线级）**：中期 §(二)5 写 "X2 优先/DeepSeek 兜底" —— 运行 `config.json`/`XF_ACTIVE_PRESET=generalv3.5`，X2 报 `11200` 未授权。评审者一验即破。须改 generalv3.5 优先并注明 X2 未授权。
- 🟡 **P1②**：中期 §(二)2 称 E5 真实向量但未披露运行降级 —— **但今日磁盘 E5 已完整恢复**（见第五节），此条已实质缓解，待确认是否真启用。
- 🟡 **P1③**：中期 §(三) 共识 0.7667→0.8333(+8.7%) 与最新 96 样本评测（trained 80.21 < rule 82.29）矛盾，属旧 30 题基准未更新。
- 🟡 **P2**：讯飞 "10 项" 实路由≈9 端点；KB 计数 2083/2113/2122 漂移；评测题数 28/30/43 漂移。

综合评级：**B+（工程真，文档待校）**。非造假，是 "未授权/降级/旧基准当现行状态写" 的口径漂移。

---

## 五、状态更新：E5 向量模型已恢复（推翻诊断 P1②"缺失"）

- 今日诊断报告（16:31）判定 `models/e5-base-v2` **缺失** → 运行走 BM25-only。
- **当前磁盘反查（21:30）**：`py-server/models/e5-base-v2/` **完整存在**：
  - `model.safetensors` 437,955,512 字节（Sep 2 17:48）
  - `config.json` / `modules.json` / `tokenizer.json` / `vocab.txt` 齐全
  - `.cache/huggingface/download` 有正常下载缓存
- **含义**：自诊断报告生成后 E5 模型已恢复，P1② 的 "缺失" 前提不再成立。
- **待验证（下一步动作）**：模型文件在 ≠ 运行期已真启用向量检索。需跑一次加载冒烟（启动后端或 `embedder` 编码一句）确认 `frugal_rag` 退出 BM25-only、关闭 `_degraded`。若确认启用，"真实向量检索" 从存储真变为运行真，卖点成立。

---

## 六、芒得很职（career-literacy）进展

- **P0 后端**：6 维 ECD 评估 + 三模式对抗（常规/加压/鲶鱼）+ 5 张新表 + 6 API（commit `5e5c6fb`）。
- **P1 学生端前端**：四步状态机 + 纯 SVG 六维雷达 + 证据链回放（commit `5065f7b`）。
- **P2→P4**：鲶鱼机制 MAPPO 化（`use_mappo_policy` 灰度关闭默认）、教师端（建班/花名册/任务码/班级看板）已完成。
- **真实 LLM 冒烟**：`career_llm_smoke_20260913.json` overall=4.5，落盘。
- 12 周路线图剩 W11-12（LangGraph interrupt HITL + 闽江学院信息安全 1 班 46 人试点）。

---

## 七、关键风险与待决项（按优先级）

1. **【高】rebase 悬挂** —— 阻塞 git 操作，须 `continue` 或 `abort`（见第二节）。
2. **【高/红线】中期报告 X2 通道（P0）** —— 文档须订正为 generalv3.5 优先并注明 X2 未授权；代码侧无需动。
3. **【中】E5 是否真启用向量检索（P1②）** —— 模型已恢复，待加载冒烟确认；若启用则卖点升级。
4. **【中】共识口径（P1③）** —— 中期 +8.7% 与最新评测矛盾，统一到 "修复退化映射、与规则基线持平" 诚实表述。
5. **【低】计数漂移（P2）** —— 讯飞能力数、KB 规模、评测题数全量统一到磁盘实测值。
6. **【环境】后端当前未运行** —— 8002 无响应；演示前需启动（约 30s 冷启）。

---

## 八、建议下一步（待你确认后我再落笔）

- **第一步**：决断 rebase（推荐 `continue` 收口）。
- **第二步**：订正中期报告 P0（X2）→ P1③（共识）→ P2（计数），按 `diagnostics/深度质检与评价-2026-09-13.md` 第四节清单逐条改。
- **第三步**：跑 E5 加载冒烟，确认向量检索真启用；若通过，README §〇 "向量检索" 由 ⚠️ 升为 ✅。
- **第四步**：启动后端做端到端演示验证。

*本盘点全部判读来自落盘反查；受检文档（中期报告 docx、README）未改动。*
