# 大改盘点 + 三条指令落地（2026-09-15 晚）

## 〇、所谓"大改"到底是什么
`career-literacy` 分支当前工作树有 **65 个未提交文件**（62 个 `src/views/*.vue` + 2 个 `py-server/tests/*` + 1 个 `Dockerfile`），
是并行会话做的两件事叠加：
1. **前端大重构 / 设计令牌 & a11y 迁移**（62 个 vue，今天 02:30 已落盘 4 commits，剩余 vue 属其他会话未提交）；
2. **MAPPO 评审策略实验**（`_c_commit_msg.txt` 描述：解析最优档位 `analytic_review_action(evidence,consensus)` + f2 阈值折进规则；
   新增 `experiments/diag_rule_upgrade.py`/`diag_state_extend.py`/`accept_review.py` + 16 条测试 `test_review_analytic.py`/`test_review_rule_upgrade.py`，均已落库）。

**关键判断**：我的三条指令落点（`pyproject.toml` / `README.md` / `references/*.docx` / `py-server/config.py` / `.env`）
**全都不在 65 改件里**，大改未碰它们 → 可安全继续，且**绝不动那 62 个 vue（别人的未提交工作）**。

## 一、凭证接入环境变量（T1）
- **打码验证**：`.env` 功能必需密钥**全部齐备**——`DEEPSEEK_API_KEY`(26) / `XF_API_KEY`(32) / `XF_APP_ID`(8) /
  `XF_API_SECRET`(32) / `XF_API_PASSWORD`(41) / `XF_SEARCH_PASSWORD`(41) / `XF_ACTIVE_PRESET=generalv3.5`。
  仅 `XF_APPID`（=XF_APP_ID 别名）与 `XF_MODEL`（有 config.json 默认值）缺失，**均为可选，不影响**。
- **接线本就对**：`config._load_dotenv()` 仅当 key 不在 `os.environ` 时才写 `.env` 值 ⇒ **OS 环境变量优先于 `.env`**，
  满足"接入我环境变量"；用户把 key 设进系统环境变量即生效，无需改代码。
- **真对话未跑**：后端当前未运行（上轮后台进程随会话结束退出），且沙箱 `http_proxy` 劫持外连，LLM 真调用在沙箱必失败。
  ⇒ 端到端对话演示**留待你在真环境 `uvicorn main:app` 起服务后验证**（凭证已齐备）。

## 二、统一闽江大学（T2）
已替换并改名（活源 + 对外一致）：
- `references/附件1：闽江大学…中期检查报告书.docx`（正文 1 处 + 改名）
- `references/附件4：闽江大学…研究报告.docx`（正文 1 处 + 改名）
- `deliverables/闽江申报材料归档-2026-09/` 下附件1/附件4 docx（同上）
- `README.md`（1 处）、`build_midterm_report.py`（3）、`scripts/build_research_report.py`（3）、`scripts/integrity_rewrite_docx.py`（1）
复扫确认：README 与 3 脚本 **0 处** 闽江学院。

**残留（已标注，不动）**：
- `.doc` 旧版 OLE 格式（references/附件1·4、deliverables/附件1·4 共 4 个）—— python-docx 无法编辑，需 Word/OLE 转换器；
- `references/三创〔2025〕12号…结题验收通知.docx` 正文含闽江学院——**学校下发官方红头文件，非我方成果物，不改**（改官方文件违背事实）。

## 三、清工程债务（T3）
- **pytest 全盘报错真因（推翻旧记忆"FastAPI Router 版本兼容"误判）**：
  `tests/crypto_platform/py-server/tests/conftest.py` 与根 `tests/conftest.py` 撞名（`tests.conftest`）
  → `ImportPathMismatchError` 收集阶段即中止，**所有测试全红**。
- **修复**：`py-server/pyproject.toml` 的 `addopts` 追加 `--ignore=tests/crypto_platform`（vendored 副本排除）。
- **验证**：收集 **878/886 通过**；`test_xfyun_services.py` **21 passed**（含凭证逻辑）；大改 review 测试 **16 passed**。
- **清我方临时文件**：`_*_tmp.py` / `_midterm_text.txt` / 运行日志 移入 `_scratch_tmp/`（非破坏）。

## 四、剩余风险 / 待你拍板
1. 后端未运行 + 沙箱网络限制 ⇒ 真对话演示需真环境。
2. `.doc` 文件闽江大学待转 OLE（非阻塞）。
3. 根目录 56 个 `_c_*.log` + `_c_commit_msg.txt` 是**别的会话**的 MAPPO 实验日志/待提交 message，**未动**（避免踩并行会话）。
4. 我的改动（pyproject / README / 3 脚本 / 4 docx）**均未提交**——避免混入别人 65 文件；提交范围等你定（建议只 add 自己的文件）。
