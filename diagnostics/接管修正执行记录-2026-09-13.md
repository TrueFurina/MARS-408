# 全量接管与风险修正执行记录（2026-09-13 续）

> 用户授权：本项目各类未完成任务全量接管 + 各类风险严格修正。
> 方法：全部落盘反查与真实运行验证，不轻信文档自述；受检文档修改前先备份；遵守"绝不删除任何东西"红线。
> 立场：本记录仅陈述已执行的动作与证据，未改动除受检文档（已备份）与 README 外的任何源码逻辑。

---

## 一、已完成的修正（8 项，均落盘可复现）

| # | 任务 | 动作 | 证据 |
|---|---|---|---|
| 1 | rebase 残留收口 | `git rebase --quit` 清理 `.git/rebase-merge` 损坏状态（2182566/9241683 对象已不可解析），HEAD 与分支保留 `3f4b66a` | git status 干净，rebase-merge 已 CLEARED |
| 2 | 中期报告 P0 红线 | §(二)5「X2 优先/DeepSeek 兜底」→「generalv3.5 优先/DeepSeek 兜底（X2 未授权 11200）」；讯飞「10 项」→「9 项」对齐路由 | `references/附件1…中期检查报告书.docx` 已改；备份 `.integrity_backup/附件1_中期检查报告书_备份_20260913.docx` |
| 3 | 中期报告 P1③ 共识口径 | §(三) 保留 30 题基准 +8.7% 真实数字，追加 96 样本诚实括注（80.2% vs 82.3% 持平，Pearson −0.15→0.22） | 段落 [32] 已追加括注 |
| 4 | 中期报告 P2 计数 | KB「2083」→「2122」（§(二)2 / §(三)数据来源 / 问题3 / 指导教师意见 共 4 处） | 与磁盘实测 2122 一致 |
| 5 | E5 真启用验证 | `embed_text` 冒烟 RESULT_OK（768 维 / norm=1.0 / 非零）；降级守卫逻辑确认 embed 成功即走向量路径 | `scripts/_e5_smoke_tmp.py` 输出 RESULT_OK |
| 6 | README 诚信口径升级 | §〇 向量检索 ⚠️→✅；计数 2113→2122（4 处）；§五 状态更新 + E5 本地化恢复注 | `README.md` 5 处替换 missing=0 |
| 7 | E5 持久化根因缓解 | 确认 `.gitignore` 排除 `e5-base-v2/`；恢复脚本 `fetch_e5_model.py`/`verify_e5.py` 固化；README 文档化「一键恢复」 | `.gitignore:145-146`；README §五 恢复注 |
| 8 | 后端启动验证 | `uvicorn :8002`，`/docs` 200，冷启 845ms，加载 2122 条向量，三级降级（Redis→内存/PG→SQLite/Milvus→InMemory）正常；对话端点 401（认证生效） | `_backend_run.log` 启动日志 |

---

## 二、遗留与待用户确认项

- **LLM 凭证（外部依赖，已知 ⚠️）**：端到端真实对话生成需 `.env` 配置 DeepSeek/generalv3.5 凭证 + 登录 admin。本轮未验证真实对话（属运行环境依赖，非代码风险）。如需演示对话，请确保 `.env` 凭证就绪。
- **校名不一致（提示，未擅自改）**：中期报告抬头/文件名用「闽江学院」，表格导师单位用「闽江大学」。用户红线要求统一「闽江大学」。表单抬头是否改请定夺（涉及填表规范与立项主体名称）。
- **工程债务（环境级，非本轮可修）**：Windows 全量 pytest 因 FastAPI Router 版本兼容报错（以 Linux CI 为准）；env 级 SIGSEGV 阻断部分测试。已记录，不在此轮修改（需 CI 环境）。

---

## 三、红线遵守声明

- 受检 docx 修改前已备份至 `.integrity_backup/`，所有修改可追溯。
- **未删除任何项目文件**（含临时工作脚本 `scripts/_*_tmp.py` 保留为可复现证据）。
- 所有对外口径（README §〇、中期报告）现已与运行实况一致（诚实三档）。

---

## 四、当前可演示状态

- 后端：http://127.0.0.1:8002 运行中（后台 task `wB5IOm`），2122 条向量已加载，E5 向量检索启用。
- 前端：`npm run dev` → :5173（代理 /api → 8002）。
- 演示前置：注册/登录 admin → 对话学习闭环（需 LLM 凭证）。

*执行记录基于落盘反查与真实运行，受检文档与 README 修改均已落盘。*
