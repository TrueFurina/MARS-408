# 真对话闭环验证 + 三项指令总收口（2026-09-15 续）

> 承接 `diagnostics/大改盘点与三条指令落地-2026-09-15.md`。本轮在 Agent 模式继续推进，补上了之前唯一未实测验证的环节，并更正一处误判。

## 一、真对话实测闭环（推翻旧误判）

**旧结论（错误）**：此前三轮均标注「真 LLM 对话受沙箱 http_proxy 劫持外连，需你在真环境 uvicorn 起服务验证」。

**实测结果（2026-09-15 12:3x 复验）**：后端 `:8002` 在跑（`/docs` 200），走 `register → Bearer token → POST /api/chat/stream` 全链路实测成功：

| 项 | 结果 |
|---|---|
| `POST /api/auth/register` | 200，返回真实 JWT（`role=student`，187 字符） |
| `POST /api/chat/stream` | 200，SSE 流式逐字返回 |
| 首字延迟 / 总耗时 | 流式 ~2.6s 完成整句 |
| LLM 真实生成内容 | 「TCP 三次握手是指客户端和服务器通过三次报文交互确认彼此的发送和接收能力均正常，从而建立可靠连接的过程。」（53 字，非模板/非降级） |
| 检索链路 | 后端加载 2122 条 E5 向量，frugal_rag 走真实向量路径（`_degraded` 关闭） |

**根因更正**：沙箱代理并未拦截后端外连——此前误判源于 `register` 路径写错（`/auth/register` vs `/api/auth/register`）导致 404，而非 LLM 不可达。后端进程继承的运行时环境可正常访问讯飞/DeepSeek。**凭证接线从 env/.env 到 LLM 生成全链路已实证打通。**

## 二、三项指令总状态（全部闭环）

| 指令 | 状态 | 证据 |
|---|---|---|
| ① 凭证接入环境变量 | ✅ 已闭环 | `.env` 功能密钥齐备；`config._load_dotenv` 保证 OS env 优先；register+chat 真跑通 |
| ② 统一闽江大学 | ✅ 已闭环（docx/README） | `git ls-files \| grep 闽江学院` = **空**；docx 正文+文件名已改、README 已改、build 脚本已改，均提交 |
| ③ 清工程债务（pytest） | ✅ 已闭环 | `tests/crypto_platform` 撞名 conftest 已 `--ignore`；`test_xfyun_services` 21 passed / 4.83s；大改 review 测试 16 passed |

## 三、唯一遗留：`.doc` OLE 旧版

- `references/附件1·附件4·*.doc`、`deliverables/闽江申报材料归档-2026-09/附件1·附件4·*.doc` 文件名仍含「闽江学院」。
- 这些是从 docx 导出的 **OLE 旧版镜像**，本环境 `soffice/libreoffice/antiword/catdoc` 均缺失，python-docx 不支持 `.doc`，**技术不可编辑**。
- 项目红线「绝不删除任何东西；只读+仅追加」→ 不删。
- **建议（需你真机执行）**：在装有 LibreOffice 的机器上
  `soffice --headless --convert-to docx "<file>.doc"` → 用 python-docx 替换正文 → 回写/重命名；或一次性 `git mv` 改名 + 转档替换正文。
- `.doc` 未进 git（git ls-files 无），属本地归档快照，不影响评审主体（评审看 docx）。

## 四、git 健康度（再次确认）

- 我的提交全部保留在历史：`894af83 docs(school): 统一"闽江大学"收尾...`、`6643ace chore(收口): ...pytest 忽略 vendored crypto_platform...` 等。
- HEAD `894af83` 为并行会话在我基础上收尾后的 tip；工作树残留 `Dockerfile` / `py-server/tests/test_career_policy_baseline.py` 为并行会话未提交改动，我未碰。
- orphan 部分克隆（`.git_broken_*` / `.git_partial_*`）已于上轮清理。

## 五、结论

三项指令 + 真对话演示 **全部实证闭环**。项目当前：凭证链路通、校名统一（docx）、pytest 债务清、E5 向量检索真启用、后端可跑真实对话。唯一待外部工具的是 `.doc` OLE 旧版校名统一，已给出可执行方案。
