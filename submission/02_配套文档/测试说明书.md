# MARS-408 系统测试说明书

> 版本：v2.0 | 日期：2026-07-20

## 1. 测试策略

采用分层测试策略，覆盖单元测试、集成测试、端到端验收测试、安全审计、导入队列五个层次。

| 层次 | 目标 | 工具 | 覆盖 |
|------|------|------|------|
| 单元测试 | Agent/Engine 模块级函数验证 | pytest | 引擎模块、质量闸门、思维导图逻辑、教学规则 |
| 集成测试 | API 端点契约 + LLM Mock | pytest + TestClient | 35 路由模块全覆盖 |
| 端到端验收 | P0 场景全流程验证 | pytest + TestClient | 模块导入、路由注册、知识图谱、LangGraph、FrugalRAG、GOMARL |
| 安全审计 | 安全加固措施验证 | pytest | 硬编码密钥检测、沙箱拦截、认证覆盖、敏感词过滤 |
| 导入队列 | 单写者 + 并发写安全 | pytest | 导入队列单元/集成/E2E/并发写 |

**总计：500+ 条测试用例，33 个测试文件，全部通过。**

---

## 2. 测试环境

| 组件 | 配置 |
|------|------|
| 操作系统 | Windows 11 / Linux (Docker) |
| Python | 3.12 |
| Node.js | 20.x |
| 向量库 | InMemoryVectorStore（测试用回退，无需外部服务） |
| LLM | Mock（`conftest.py` 全局注入，不依赖真实 API） |
| 认证 | Mock（`conftest.py` 全局覆盖，自动以 admin 身份通过） |
| 持久化 | SQLite 临时文件（`tmp_path`，自动回收） |

---

## 3. 测试用例说明

### 3.1 API 契约测试（`test_api_contract_comprehensive.py`）

验证所有 71+ API 路由的可达性和响应格式。

**测试用例分类：**

**基础可达性测试（60+ 条）：** 每个端点返回 `!= 404`
```
GET  /api/status          → 200 OK
POST /api/auth/login      → 200 + token
POST /api/auth/register   → 200 + token
GET  /api/auth/me         → 200 + user
POST /api/chat/send       → 200 + response
POST /api/chat/stream     → 200 + text/event-stream
POST /api/rag/search      → 200 + results
POST /api/knowledge/graph → 200 + {nodes, edges}
...
```

**画像构建测试：** 验证对话式画像 ≥6 维度完成
```
test_profile_build_completes:
  mock LLM 返回 8 维画像 → completed=true
```

**流式端点测试：** SSE 格式验证
```
test_endpoint_reachable[POST /api/chat/stream]:
  content-type = text/event-stream
  body 包含 "data:"
```

### 3.2 E2E 验收测试（`test_e2e_p0_acceptance.py`）

覆盖赛题 P0 功能的 15 个验收场景：

| 编号 | 测试 | 验证点 |
|------|------|--------|
| T1 | 模块导入 | 19 个核心模块无 ImportError |
| T2 | API 路由 | 71 条路由全部注册 |
| T3 | 知识图谱 | POST → 返回 {nodes, edges}，subject 过滤正确 |
| T4 | LangGraph SSE | astream 模式正确，节点事件推送 |
| T5 | FrugalRAG 检索 | 返回结果含 score/text/metadata |
| T6 | GOMARL 共识 | 评分+冲突检测+决策全流程 |
| T7 | NeuralMixer | 神经网络混合可用 |
| T8 | Agent 辩论 | debate 协议可执行 |
| T9 | 教学规则 | 前置依赖校验+Agent适配 |

### 3.3 引擎模块测试（`test_engine_modules.py`）

验证各算法引擎核心功能：

**FrugalRAG：**
```
test_preprocess_removes_noise    → 查询预处理
test_extract_keywords_network    → 关键词提取
test_assess_complexity_simple    → 简单/复杂查询分类
test_stop_decision_high_coverage → 停止决策阈值
test_rewrite_with_llm           → 查询重写
test_rewrite_fallback_no_llm    → 无 LLM 时降级
```

**GOMARL：**
```
test_initial_weights          → 基础权重初始化
test_mix_with_mock_results    → 神经网络混合
test_mix_fallback_no_torch    → 无 torch 降级
test_check_and_resolve_no_conflicts  → 无冲突检测
```

**Neural GroupMixer：**
```
test_initial_weights        → 初始权重非零
test_get_stats              → 统计信息完整
test_dynamic_weights        → 画像调整权重
test_group_detection        → 动态组划分
```

### 3.4 安全审计测试（`test_audit_fixes.py`）

验证安全加固措施的有效性：

```
test_no_hardcoded_secrets         → 无硬编码密钥
test_save_config_strips_secrets   → 配置保存自动剥离密钥
test_sandbox_blocks_dangerous     → 沙箱阻止危险调用
test_auth_all_endpoints           → 关键端点需认证
test_content_filter_active        → 敏感词过滤启用
```

### 3.5 知识图谱测试（`test_knowledge_graph.py`）

```
test_knowledge_graph_basic_schema     → 含 nodes/edges 字段
test_knowledge_graph_node_fields       → 节点含 id/label/group
test_knowledge_graph_edge_fields       → 边含 source/target
test_knowledge_graph_subject_filter    → 按科目过滤正确
test_knowledge_graph_all_subjects_non_empty → 四科都有数据
```

### 3.6 会话管理测试（`test_api_consistency.py`）

```
test_list_empty           → 空列表返回 []
test_list_after_save      → 保存后可列出
test_save_load_cycle      → 保存→加载内容一致
test_delete_removes       → 删除后不存在
```

---

## 4. Mock 与桩件

### 4.1 LLM Mock

所有依赖 LLM 的测试使用 `conftest.py` 中的 `mock_llm` 自动 fixture：

```python
def mock_llm(monkeypatch):
    # 覆盖 chat：返回8维度画像JSON
    # 覆盖 stream_chat：返回合法SSE chunk + [DONE]
    # 覆盖 text_completion：返回非空 mock 响应
    # 覆盖 _resolve：返回可用通道配置
```

### 4.2 认证 Mock

所有 API 测试使用 `mock_auth` 自动 fixture：

```python
def mock_auth():
    # 创建 test_admin 用户
    # 覆盖 get_current_user → admin 身份
    # 覆盖 require_admin → admin 权限
```

### 4.3 会话隔离

```python
def _temp_sessions(monkeypatch, tmp_path):
    # 将会话目录重定向到临时目录
    # pytest 自动回收，零副作用
```

---

## 5. 测试覆盖度

### 5.1 代码覆盖

| 模块 | 测试文件 | 覆盖要点 |
|------|----------|----------|
| api/auth.py | test_api_contract_comprehensive | 注册/登录/登出/me |
| api/chat.py | test_api_contract_comprehensive | send/stream 端点 |
| api/agents.py | test_api_contract_comprehensive | 资源生成端点可达 |
| api/rag.py | test_api_contract_comprehensive, test_e2e | 搜索+生成 |
| api/quiz.py | test_api_contract_comprehensive, test_e2e | 提交+带参检查 |
| api/assessment.py | test_api_contract_comprehensive | 评估端点 |
| api/learning_path.py | test_api_contract_comprehensive | 路径端点 |
| api/knowledge.py | test_api_contract_comprehensive, test_knowledge_graph | CRUD+图谱 |
| api/sessions.py | test_api_consistency | 保存/加载/列表/删除 |
| api/sandbox.py | test_audit_fixes | 安全拦截 |
| api/config_routes.py | test_audit_fixes | 密钥剥离 |
| engines/frugal_rag.py | test_engine_modules | 检索/预处理/停止决策 |
| engines/gomarl.py | test_engine_modules, test_e2e | 共识评估 |
| engines/gomarl_mixer.py | test_engine_modules | 神经网络混合 |
| engines/gomarl_conflict.py | test_engine_modules | 冲突消解 |
| engines/agent_debate.py | test_e2e | 辩论协议 |
| engines/teaching_rules.py | test_engine_modules | 前置依赖/Agent适配 |

### 5.2 功能覆盖

| 赛题功能 | 测试覆盖 | 用例数 |
|----------|----------|--------|
| ① 画像构建 | 端点可达 + Mock 完成 | 2 |
| ② 多智能体资源生成 | 非流式+流式端点可达 | 3 |
| ③ 学习路径 | 端点可达 + 参数检查 | 2 |
| ④ 智能辅导 | 端点可达 | 1 |
| ⑤ 学习效果评估 | 端点可达 + 结构检查 | 2 |
| 非功能-防幻觉 | AST分析 + 敏感词过滤 | 3 |
| 非功能-SSE流式 | content-type + body 格式 | 3 |

### 5.3 边界/异常覆盖

| 场景 | 测试 | 覆盖 |
|------|------|------|
| LLM 不可用 | mock_llm 模拟失败 | ✅ |
| 向量库为空 | 种子数据检查 | ✅ |
| 无 torch 降级 | NeuralMixer fallback | ✅ |
| 认证过期 | mock_auth 模拟 | ✅ |
| 无效 conv_id | 正则拒绝 | ✅ |
| 沙箱攻击 | AST Blocker | ✅ |

---

## 6. CI 集成

测试套件可在无外部服务依赖的环境中独立运行：

```bash
cd py-server
pip install -e ".[test]"
pytest tests/ -v --tb=short
```

**运行时间**：约 30-40 秒（500+ 条用例）
**输出格式**：pytest 标准 + 3 个 DeprecationWarning（均为第三方库，非项目代码产生）

---

## 7. AI Skills 平台测试

### 7.1 API 端点测试

| 端点 | 方法 | 测试内容 | 状态 |
|------|------|---------|:----:|
| `/api/skills/create` | POST | 创建技能（含JSON body） | ✅ |
| `/api/skills/get/{id}` | GET | 获取技能详情 | ✅ |
| `/api/skills/update/{id}` | POST | 更新技能（含JSON body） | ✅ |
| `/api/skills/delete/{id}` | POST | 删除技能 | ✅ |
| `/api/skills/publish/{id}` | POST | 发布技能 | ✅ |
| `/api/skills/archive/{id}` | POST | 归档技能 | ✅ |
| `/api/skills/market` | GET | 市场浏览（过滤/搜索/排序） | ✅ |
| `/api/skills/my` | GET | 我的技能列表 | ✅ |
| `/api/skills/official` | GET | 官方技能列表 | ✅ |
| `/api/skills/rate/{id}` | POST | 评价技能 | ✅ |
| `/api/skills/ratings/{id}` | GET | 评价列表 | ✅ |
| `/api/skills/favorite/{id}` | POST | 收藏技能 | ✅ |
| `/api/skills/unfavorite/{id}` | POST | 取消收藏 | ✅ |
| `/api/skills/favorites` | GET | 收藏列表 | ✅ |
| `/api/skills/favorited/{id}` | GET | 检查是否收藏 | ✅ |
| `/api/skills/stats` | GET | 创作者统计 | ✅ |
| `/api/skills/templates` | GET | 模板列表 | ✅ |
| `/api/skills/from-template/{id}` | POST | 从模板创建 | ✅ |
| `/api/skills/run/{id}` | POST | 技能运行时 | ✅ |
| `/api/skills/run-stream/{id}` | POST | 流式技能运行时 | ✅ |
| `/api/skills/prompt-test` | POST | Prompt 测试 | ✅ |

### 7.2 增强功能测试

| 模块 | 测试内容 | 状态 |
|------|---------|:----:|
| media_generator | 视频脚本/SVG/Mermaid 生成 | ✅ |
| feedback_agent | 学习效果评估 + 路径调整 | ✅ |
| tutor_enhanced | 多模态答疑（文字+SVG+视频） | ✅ |
| benchmark | 性能基准测试（9项） | ✅ |

### 7.3 测试命令

```bash
# 运行所有测试
cd py-server
pytest tests/ -v --tb=short

# 运行 AI Skills 专项测试
python -c "from db.skill_store import *; # 手动验证存储层"

# 运行性能基准测试
python -m scripts.benchmark
```
