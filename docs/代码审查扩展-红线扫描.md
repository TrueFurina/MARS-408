# 代码审查扩展报告（红线扫描）· `main` 分支

> **依据**：《代码审查标准.md》§0/§2.1/§2.2/§3
> **范围**：`main`（MARS-408 火山杯作品）红线关键词扫描 + 模块速查建议
> **审查人**：火眼眼 ｜ **日期**：2026-09-16
> **说明**：本批为"扩展审查"，用 grep 对红线关键词做全仓扫描，定位真实合规面与缺口。完整逐文件审查（frugal_rag.py / gomarl.py 等）留待后续批次。

---

## 一、红线扫描结果

### 1. embedding 维度 / MiniLM 扫描 —— 5 命中，**0 违规** ✅

| 文件 | 命中 | 性质 |
|---|---|---|
| `engines/gomarl_mixer.py:156,371,393` | `embed_dim=384` | MARL mixer **网络内部**嵌入维度，非检索文本 embedding |
| `export_onnx.py:41,115` | `embed_dim=384` | mixer ONNX 导出参数 |

- 红线 §2.1① 精准限定"**检索层**须 `e5-base-v2`/768"，mixer 内部维度不在其列。
- 💡 **标准补强建议**：在 §2.1① 加一句"此约束仅限检索/向量库路径；MARL 网络内部维度（如 mixer 384）除外"，进一步防审查者误拦。

### 2. 前端裸 `fetch` 扫描 —— 🔴 **重大缺口**

`main` 前端存在大量组件/视图层裸 `fetch`（违反 §2.2② 禁裸 fetch、须走 `utils/api.ts` 统一鉴权/退避/友好错误）：

| 文件:行 | 说明 |
|---|---|
| `components/CompareProfilesPanel.vue:45,50` | 裸 fetch `${API_BASE}/api/engine/frugal-rag-full` |
| `views/KnowledgeAdminView.vue:73,109,172,179,190,212,228,242,252` | **11 处**，且手写 `getAuthHeaders()`，与 `api.ts` 鉴权不一致 |
| `components/TeachingRulesPanel.vue:16,25` | 裸 fetch engine 接口 |
| `views/ResourceView.vue:181` | 相对路径 `/api/multimodal/...`，**无 baseURL/鉴权**，最危险 |
| `views/SkillDetailView.vue:76` | 裸 fetch |

- **合法豁免**（符合标准）：`utils/api.ts:79,144,174,189,216`（封装层本身）、`workers/cppCompiler.worker.ts:42`（CDN 拉取，标准明示豁免）。
- ⚠️ **对比**：`career-literacy` 分支已于 2026-09-15 P0 改造**清零裸 fetch**；`main` 作为"冻结基线"反而存在更严重的裸 fetch——参赛作品前端红线未落地。
- 🔴 **建议**：参赛前修复。最小集 = `ResourceView.vue:181`（相对路径无鉴权）+ `KnowledgeAdminView` 手写鉴权不一致；完整修复 = 全部改走 `api.ts`（复用 career 已验证的 P0 方案）。注：ADR-015 的"main 冻结"是防 career 污染 408，**不禁止修 main 自身红线 bug**；但改动建议单独 PR 并由用户授权（见下方"待用户决策"）。

### 3. 无声吞错扫描（`except: pass`）—— **0 命中** ✅

- `main` 后端无 `except: pass` 无声吞错，符合 §2.1⑨ 红线。👍

---

## 二、模块速查建议（后续批次待审）

- `engines/frugal_rag.py` / `gomarl.py` / `gomarl_mixer.py`：建议做完整审查（关注点分离、并发写者、量纲 0–1 vs 0–100、skip 纪律）。
- 前端 `ResourceView.vue`(1324 行) / `DashboardView.vue`(1210 行)：单文件过大（§2.2⑥ 反面案例），建议拆分 composable/子组件。
- 在 `main` 重做 career 的"前端红线 P0 改造"（裸 fetch→api.ts）是一项明确的参赛前技术债清理，建议立项。

---

## 三、结论

- **核心后端红线**（检索 embedding 模型、无声吞错）在 `main` 合规。
- **主要缺口在 `main` 前端裸 fetch**（未做 P0 治理），属 🔴 但可修复；建议单独 PR。
- 下一步：待用户授权后，可在 `main` 启动"前端裸 fetch→api.ts"整改；或先对 `frugal_rag.py`/`gomarl.py` 做逐文件完整审查。

---

## 待用户决策

1. **是否授权在 `main` 修前端裸 fetch**（建议单独 PR，先做最小集：ResourceView 相对路径 + KnowledgeAdminView 鉴权）。
2. **是否继续逐文件完整审查** `frugal_rag.py` / `gomarl.py`（本轮仅做关键词扫描）。
3. **GitHub push 当前被沙箱代理拦截**（github.com:443 over proxy 127.0.0.1 连接失败）—— commit 已落在本地 `main`（ahead 2），待网络恢复或你确认代理设置后我再推送。

---
## 状态更新（2026-09-15）
上述 main 裸 fetch 🔴 已全部修复并提交（commit `65990eb`）：17 处裸 fetch 改走 `src/utils/api.ts`，仅保留 2 处合法豁免（api.ts 封装本体、cppCompiler.worker.ts 的 CDN 拉取）。main 与 career 前端请求层现已一致收敛到统一封装。

## 状态更新（2026-09-16）
核心算法深度审查已完成：`docs/代码审查-核心引擎深度审查.md`。逐行实质审查 6 文件（`frugal_rag.py`/`gomarl.py`/`gomarl_mixer.py`/`gomarl_conflict.py`/`agents/graph.py`/`api/engine.py`）。结论：**0 🔴**（embedding 检索层 768 维硬约束在算法层遵守；mixer `embed_dim=384` 为死默认值，生产由 config=768 覆盖，非违规）。重点 🟡：`C3` 语义级冲突检测在生产 API 不可达（文档宣称≠实际接线）、`C6` 跨模块私有 API 耦合（`teaching_rules._dependencies` 5 文件 20+ 处）、`C9` 只读端点误挂 LLM 配额。建议补标准 §0/§3 共 7 条。
