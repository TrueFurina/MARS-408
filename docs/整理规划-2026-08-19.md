# 项目根目录整理规划（2026-08-19）

> 状态：**✅ 已于 2026-08-19 执行完毕**（用户确认后按 §5 顺序执行）。基线 commit `9e41e6c` → `reorg(a)` `d6b73fb` → `reorg(b)` `96964ee` → `reorg(d)` `dafb505` → 脚本修复 `cdcfc0d`。
> 执行结果：根目录从 40 目录 + 60 散落文件收敛为 13 个业务目录 + 标准配置；`08013417*/deliverables/design-system` 归档至 `archive/` 并从 git 索引退役（历史保留）；3 个脚本路径已同步更新；`build_source_zip.py` 移至 `scripts/` 后修复了 ROOT 解析（曾误输出空 zip）。
> 验证：type-check exit 0；`check_submission_ready.sh` 6/6 通过；源码 zip 4.3MB/397 条目重建成功；docker-compose config 无路径错误；git status 干净。
> 遗留：`nul`（0 字节 Windows 设备名残留，沙箱回收站无法处理，可手动 `del "\\?\E:\Program\MARL\study-help-pro\nul"` 或忽略）；`build-only` 因 vite dev server 占用 dist 无法在当前会话跑（type-check + 此前构建已验证前端无回归）。

---

## 1. 问题诊断（一句话）

根目录 40 个目录 + 60 个散落文件，把 **活动源码 / 参赛快照 / 构建产物 / 一次性日志 / 历史报告** 五类东西平铺在了一起。

量化证据：

| 类别 | 条目 | 文件数 |
|---|---|---|
| 构建产物 | `dist` + `dist-qa/rem/test/verify/vuecheck` + `build` | ~4,600 |
| 参赛快照 | `08013417介绍/作品/报名/源码` | ~1,200 |
| 历史文档 | `deliverables` + `documents` + 顶层 12 份报告 | ~560 |
| 一次性日志/脚本 | 6 个 `*.log` + 6 个 `_*.py` + `smoke_*` + `nul` 等 | ~20 |
| 启动脚本 | `start.*` ×3 + `start_demo.*` ×2 + `start_netlearn.*` ×2 + `stop.*` ×2 + `recover.bat` | 10 |

---

## 2. 依赖探查结论（红线清单）

以下路径**不可移动/不可删除**（有运行/构建/部署依赖），规划中一律保留原位：

| 路径 | 依赖方 | 原因 |
|---|---|---|
| `dist/` | `start.bat:50`、`start.ps1:44`、`serve_spa.py:5` | 前端构建输出，启动脚本检查 `dist/index.html` |
| `src/` `public/` `index.html` `vite.config.ts` `tsconfig*.json` `env.d.ts` | `Dockerfile:11-17`、vite | 前端源码 |
| `py-server/` | `Dockerfile:45-51`、CI pytest | 后端源码 |
| `documents/` | `docker-compose.yml:53`（`./documents:/app/documents:ro`）、`build_portable.py:121` | 挂载卷 + 便携包教材来源；挪动须同步改两处配置 |
| `submission/` | `build_source_zip.py:9`（输出 `submission/04_源码/MARS-408_source.zip`） | 交付包输出目标 + git 已跟踪 69 文件 |
| `.github/` | CI | 只跑 pytest + gitleaks，不依赖待归档目录（已核实） |
| `node_modules/` `package-lock.json` | npm | 依赖 |

**已核实安全**（无任何脚本/CI/Docker 引用，可安全归档）：
- `dist-qa/` `dist-verify/` `dist-vuecheck/` `build/` —— git 跟踪数均为 0（纯本地产物）
- 全部顶层 `*.log` —— 已被 `.gitignore` 的 `*.log` 忽略
- `nul` —— 已入 `.gitignore`，Windows 空设备残留，纯垃圾
- `_verify_*`、`_deep_qa_test*.py`、`_qa_endpoint_test.py` 等 `_` 前缀脚本

**关键事实**：`build_source_zip.py:16-18` 已明确把 `archive`、`documents`、`deliverables`、`design-system`、`dist-qa/rem/test/verify/vuecheck`、`submission` 列为源码打包**排除项**——即这些目录本就属于「非源码交付物」，归档它们**不会**影响源码 zip 生成。

---

## 3. 目标目录树（整理后）

```
study-help-pro/
├─ src/  py-server/  public/  dist/  node_modules/        # 运行核心（不动）
├─ documents/  docs/  references/                          # 文档（documents 不动）
├─ scripts/  tools/                                        # 工具脚本
├─ submission/                                             # 交付包（不动）
├─ archive/                                                # 历史快照（gitignore 已忽略）
│  ├─ 08013417_参赛快照/    # ← 08013417介绍/作品/报名/源码
│  ├─ dist_旧构建/          # ← dist-qa/rem/test/verify/vuecheck + build
│  ├─ logs/                 # ← 全部 *.log
│  ├─ scratch/              # ← _*.py 一次性脚本 + smoke_* + wasi-*.mjs
│  └─ experiments/          # ← experiments/results
├─ design-system/  → 并入 docs/design-system/（或保留，见 D-2）
├─ 根目录保留：
│  ├─ README.md  CLAUDE.md  overview.md  INSTALL.md
│  ├─ start.bat  start.sh  start.ps1            # 唯一启动入口
│  ├─ serve_spa.py
│  ├─ package.json  vite.config.ts  tsconfig*.json  env.d.ts  index.html
│  ├─ Dockerfile  docker-compose.yml  .dockerignore
│  ├─ .github/  .gitignore  .gitleaks.toml  .pre-commit-config.yaml  .prettierrc.json
│  └─ 静态配置：postcss.config.js  eslint.config.js  vitest.config.ts
```

---

## 4. 全量处置分类表

### A 级 · 立即归档到 `archive/`（未跟踪，零风险，可直接执行）

| 条目 | 去向 | 操作 |
|---|---|---|
| `dist-qa/` `dist-verify/` `dist-vuecheck/` `build/` | `archive/dist_旧构建/` | `mv` |
| `dist-rem/` `dist-test/`（已被 gitignore，仍占磁盘） | `archive/dist_旧构建/` | `mv` |
| `_backend_run.log` `backend_run.log` `backend_run_qa_sweep.log` `frontend_run.log` `_vite_run.log` `_verify_collab_out.txt` | `archive/logs/` | `mv`（gitignore 已忽略） |
| `smoke_ppt.pptx` `smoke_req.json` `smoke_req2.json` `smoke_sse.log` | `archive/scratch/` | `mv` |
| `_deep_qa_test.py` `_deep_qa_test_v2.py` `_deep_qa_v3.py` `_qa_endpoint_test.py` `_gen_excalidraw.py` `_md2docx.py` `_unify_numbers.py`（保留副本至 `scripts/`） | `archive/scratch/` | `mv`（`_unify_numbers.py` 另存 `scripts/`） |
| `wasi-verify.mjs` `wasi-node-verify.mjs` `tti_test_tcp.png` `recover.bat` | `archive/scratch/` | `mv`（注意：`wasi-*.mjs` 被 git 跟踪，需 `git rm --cached` 或 D 级确认） |
| `MARS-408_软件杯演示.pptx` `MARS-408_软件杯演示_v2.pptx` `MARS-408-architecture.excalidraw` `MARS-408-architecture.html` `MARS-408-architecture.template.html` `microservice-architecture.excalidraw` `microservice-architecture.html` `netlearn_arch_gen.py` | `archive/scratch/` | `mv`（单页演示产物，已在 `submission/` 有正式版） |
| `screenshots/` `experiments/` | `archive/` | `mv` |
| `vectordb_data/` | `archive/`（或保留） | **C 级确认**：若为运行时向量库目录，保留；否则归档 |

### B 级 · 归口移动（git 跟踪，用 `git mv` 保留历史）

| 条目 | 去向 |
|---|---|
| 顶层 12 份报告：`DESIGN*.md`(×5) `DESIGN_AUDIT_REPORT*` `QA_REPORT_*`(×2) `SRE_*`(×2) `SECURITY_AUDIT_REPORT.md` `EVALUATION_ENV.md` `PRODUCTION_READINESS_2026-07-12.md` `UI_BEAUTIFY_GAPS_2026-07-17.md` | `docs/reports/` |
| `start_demo.bat` `start_demo.sh` `start_netlearn.bat` `start_netlearn.sh` `stop.bat` `stop.sh` `recover.bat` | `scripts/`（根目录仅留 `start.bat` `start.sh` `start.ps1`） |
| `build_portable.py` `build_source_zip.py` | `scripts/` |
| `开发文档案例与编写指南/` `国家标准-软件开发规范/` | `references/`（外部参考资料归口） |

### C 级 · 保留原位（红线，见 §2）

`dist/` `node_modules/` `src/` `py-server/` `public/` `documents/` `submission/` `.github/` 及全部构建/运行配置。

### D 级 · 需用户逐项确认（git 已跟踪，归档=从版本控制退役）

| 条目 | 跟踪文件数 | 建议 | 理由 |
|---|---|---|---|
| `08013417介绍/` | 46 | 归档 `archive/08013417_参赛快照/` + `git rm -r --cached` | 参赛打包快照，正式版在 `submission/`；git 历史可回溯 |
| `08013417作品/` | 243 | 同上 | 同上 |
| `08013417源码/` | 327 | 同上 | 同上（内含嵌套 `src/src/`，是旧源码副本，已与活动源码对齐修复） |
| `08013417报名/` | 5 | 同上 | 报名表/照片 |
| `deliverables/` | 273 | 归档 + `git rm -r --cached`；**但保留** `deliverables/_unify_changelog.md` 副本至 `docs/` | 历史交付/审计记录，多数已被 `submission/` 取代；部分「记录类」文档按统一口径报告建议保留原貌，故仅归档不删 |
| `design-system/` | 10 | 并入 `docs/design-system/` | 与 `docs/` 功能重叠 |
| `references/` | 22 | 保留并接收外部参考 | 已是参考归口 |

### E 级 · 删除（仅纯垃圾）

| 条目 | 理由 |
|---|---|
| `nul` | Windows 空设备残留，已入 `.gitignore`，无内容 |

---

## 5. 执行顺序（先验证依赖 → 再动 → 再验证）

1. **冻结**：确认 git 工作区干净（`git status` 仅预期改动），记录当前 `git rev-parse HEAD`。
2. **建归档骨架**：`mkdir -p archive/{08013417_参赛快照,dist_旧构建,logs,scratch,experiments}`。
3. **A 级执行**（纯 `mv`，无 git 影响）→ 立即 `git status` 确认无意外变更。
4. **D 级确认后**：对 08013417*/deliverables/design-system 依次 `git rm -r --cached <path>` 再 `mv` 归档；**归档前**先 `cp -r` 一份到 `archive/` 之外的临时区校验完整性（或直接用 `git mv` 到 `archive/` 后 `git rm --cached` 保留工作区文件）。
5. **B 级执行**：`git mv` 报告与脚本（保留历史）。
6. **全量验证**（见 §6）。
7. **收尾**：更新 `README.md` 目录结构说明 + `.gitignore` 增加 `vectordb_data/`（若归档）。

---

## 6. 验证清单（整理后必须全过）

- [ ] `npm run build-only` → 生成 `dist/` 无报错
- [ ] `npm run type-check`（`vue-tsc --build`）exit 0
- [ ] `start.bat` / `start.sh` 路径逻辑不变（只引用 `dist/`，不受影响）
- [ ] `serve_spa.py` 可启动（`python serve_spa.py` 起本地服务）
- [ ] `docker-compose config` 校验通过（`documents/` 挂载未动）
- [ ] `build_source_zip.py` 仍能产出 `submission/04_源码/MARS-408_source.zip`（排除项不涉及新增归档目录冲突）
- [ ] `git status` 无意外删除/漂移；`git log --stat` 可回溯被归档内容
- [ ] 抽查 `README.md`/`CLAUDE.md` 提到的路径仍有效

---

## 7. 回滚方案

- 全程**只移动不删除**（E 级 `nul` 除外），任何误操作可从 `archive/` 或 `git mv` 历史恢复：
  - A 级：`mv archive/xxx 根目录/` 即回滚。
  - B 级：`git mv` 已提交则 `git log` 找回；未提交则 `git checkout HEAD -- <path>`。
  - D 级：`git rm --cached` 仅移出跟踪索引，工作区文件仍在 `archive/`；如需恢复跟踪 `git add archive/08013417_参赛快照/`。
- 若验证 §6 任一失败：立即停止，按上述规则回滚，先修依赖再重排。

---

## 8. 待用户拍板项

1. **D 级整体**：`08013417*`（621 文件）+ `deliverables`（273）+ `design-system`（10）是否同意「归档 + 从 git 跟踪退役」？（git 历史不受影响）
2. `_unify_numbers.py` 是否保留到 `scripts/`（我建议留，它是历史口径统一工具）。
3. `vectordb_data/` 是运行时向量目录还是可归档？——决定 A-末项。
4. 根目录最终只留 `start.bat/sh/ps1` 三入口，`start_demo.*`/`start_netlearn.*` 全部移入 `scripts/`，是否 OK？
5. `wasi-*.mjs`（git 已跟踪）随 A 级归档还是 D 级确认？

---

*规划完毕。确认后按 §5 顺序执行，先做 A 级（零风险），D 级逐项确认。*
