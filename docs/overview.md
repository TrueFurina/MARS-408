# 工程产出概览｜2026-08-30 04:35

## 任务

用户指出平台仍是 408 软件杯内容（无法向评审交代）→ 完成**密码学化改造**，平台已可演示。

## 改造完成（4 项，全部验证）

| 项 | 改动 | 验证结果 |
|---|---|---|
| **语料替换** | 新建 `seed_data_crypto.py`（39 知识 chunk + 24 题 + 9 科目 + KG + 学习路径），9 文件 10 处 import 切换；408 向量缓存**移到 `_backup_408/`（未删除）** | collection 2113→**63**，检索命中 SM4/SM2/ECC |
| **诊断题库** | `api/diagnostic.py` 硬编码 20 道 408 题 → 20 道密码学题（8 科目） | 诊断首题"7x≡1 mod 11"，科目=数论/对称/公钥/哈希/国密/可证明/协议/前沿 |
| **前端品牌** | index.html title/description、App.vue、LoginView（含"软件杯 A3 赛道"徽标）、LandingView 全部替换 | 首页 title=「密码学"理论—实践"智能督导与融合培养系统」 |
| **构建与挂载** | 修复 dist 锁冲突（停后端再 build），SPA 重新挂载 | `http://127.0.0.1:8012/` 200 |

## 平台现状（演示口径）

- 品牌：密码学"理论—实践"智能督导与融合培养系统（无 408/软件杯字样）
- 知识库：63 条密码学语料（SM2/SM3/SM4/SM9/ZUC、RSA/ECC、可证明安全、PKI/MPC/HE/ZKP、抗量子/区块链/DKG）
- 诊断：20 道密码学入学测评
- LLM：真实回答 SM2 签名原理（三通道容灾）
- 检索：BM25 命中密码学内容（E5 未装，走降级守卫，展示够用）

## 遗留（如实）

1. **E5 嵌入未装**（sentence-transformers）——检索走 BM25 降级，生产需 `pip install sentence-transformers` + 重载
2. **非品牌文件的 408 文案**：src ~30 处 / py 102 处（EngineView/ProfileView 等内部文案），评审不直接看，但提交前建议清
3. **嵌套 crypto_platform 副本**：未动（红线）
4. **Agent 裁剪 / 六维画像评分逻辑**：#6 剩余

## 下一步建议

- 演示优先级：E5 嵌入（检索精度）> 剩余 408 文案清理 > Agent 裁剪/六维逻辑
- 均可随时继续

> 由工程保障团队主理人（一体化模式）生成。
