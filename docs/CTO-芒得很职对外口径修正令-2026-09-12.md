# CTO · 芒得很职对外口径修正令（数字治理）

> 日期：2026-09-12 ｜ 角色：CTO ｜ 依据：`docs/CTO-芒得很职技术基线盘点-2026-09-12.md`
> 性质：**红线动作**——所有对外材料（README / 改造方案 / PPT / 申报书）执行本令，不执行不许提交

---

## 一、修正对象（4 处）

| # | 位置 | 原文表述 | 修正为 |
|---|---|---|---|
| 1 | 改造方案 P1 验证列 | "真实LLM评估 overall **4.33**" | "离线 ECD 验证通过（mock，`_p2_verify_evidence.py` 8 断言）" |
| 2 | 改造方案 P2 验证列 | "真实LLM冒烟 overall=**4.27**，六维7条原话引用" | 同上（数字删除，口径统一） |
| 3 | README career 段 | "HTTP端到端联调通过：真实LLM对抗追问 + LLM六维评估" | "HTTP 端到端联调通过（mock/离线）；真实 LLM 冒烟待落盘后补" |
| 4 | 改造方案 P0 验证列 | "兜底闭环测试通过" | "验证脚本通过（`_verify_career_p0.py`）；正式测试待 P3① 补建" |

## 二、数字治理规则（此后永久生效）

1. **无落盘不出数字**：任何"真实 LLM 冒烟/实测/准确率"必须附带日期 JSON 结果文件（`experiments/results/*.json`），否则按"待验证"口径
2. **一数一源**：同一指标全库只允许一个数值、一个出处；发现矛盾（如 4.33 vs 4.27）立即按"更早版本作废，重新实测"处理
3. **口径分级**：离线验证（mock）与真实运行（LLM）在对外表述中必须显式区分

## 三、证据重建指挥（额度/执行轮次执行）

- 升级 `_smoke_career_llm.py`：补断言（六维齐全 / 0≤score≤5 / 证据链非空）+ 输出 `experiments/results/career_llm_smoke_YYYYMMDD.json`
- 落盘后：README 第 3 处改回"真实 LLM 冒烟 overall=X.XX（YYYY-MM-DD 落盘）"，数字以落盘为准

## 四、验收

- 全库 grep：`4.27`、`4.33` 在对外材料（除本令与盘点文档）中不再出现
- README / 改造方案可复查
