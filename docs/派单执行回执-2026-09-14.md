# 三项派单执行回执（2026-09-14）

> 依据 `docs/CTO-深度验收结论与三项派单令-2026-09-14.md`。执行顺序：派单一 → 派单三 → 派单二。

---

## 派单一（🔴 career 规则基线失效）— ✅ 完成

**根因（实跑复核，非采信报告）**：`career_state_features` 与生产规则 `_decide_adversary_mode_rule` 的"无证据轮"证据密度默认 **0.5** ≥ 规则阈值 **0.35** ⇒ 规则死锁 `normal`、escalating/catfish 永不触发。
（附：B 报告"合成 env 100% normal"经复跑证实为**旧环境**结论——当前合成 env 规则已正常触发；真死锁点在 `career_state_features` / 生产规则的默认值。）

**修复（"对齐默认值"，不改规则判据语义）**
| 文件 | 改动 |
|---|---|
| `engines/career_policy.py` | 新增常量 `EVIDENCE_DENSITY_DEFAULT=0.3`；`career_state_features` 密度默认 `0.5→0.3`；`dimension_variance` 无六维评估时改用最近轮密度方差（修状态退化维） |
| `agents/career_nodes.py` | 生产规则 `_decide_adversary_mode_rule` 密度默认 `0.5→0.3` |

**测试**：新增 `tests/test_career_policy_baseline.py`（9 例）＝ CTO §6.1 基线门三条——
a) 基线会动（env 触发率 ≥10%）；b) 基线有效（低密度→加压、高密度→normal）；c) 随机性可控（同 seed 逐位一致）。

**重跑**：`experiments/results/career_mappo_train_20260914.json`（steps=3000，seeds 1/42/2024）
- **mappo reward 全超 rule**：`-0.091 / -0.103 / -0.081` vs `-0.138 / -0.127 / -0.127`
- `discipline_violations=0`，`mappo_reward_ge_rule=true`，`passed=true`
- 含 §6.2 meta：`baseline_checked=true`、`baseline_trigger_rate=0.81`、`seed_reproducible=true`、`environment_source=synthetic`

**验收对照**
- [x] `tests/test_career_policy_baseline.py` 全绿（55 passed，含既有 46）
- [x] 修复后规则版触发率 **0.81 ≥ 10%**（不再 100% normal）
- [x] 3-seed 对比 JSON 落盘且 `mappo ≥ rule` 结论在新基线下**成立**
- [x] 全量回归 **843 passed / 0 errors**（基线 834 + 新 9 例，只增不减）

---

## 派单三（🟡 career 增益口径）— ✅ 完成

- 全仓 `*.md` grep 核对：**无"六维均分提升"声称**（外材料零命中）。
- 口径规则写入 `docs/验收记录-三元评审权重MAPPO-2026-09-14.md` §七：
  - ✅ 可报：奖励超规则（3-seed）、证据密度 0.41→0.55（+27% 量级）、纪律违规 0 —— 均注明"合成环境（卡住型学生模拟）"。
  - ❌ **禁止**：六维均分 +0.02（噪声级，不得写"六维均分提升"）。

---

## 派单二（🟡 novelty 叙事重构）— ✅ 完成

- `docs/CTO-芒得很职三元评审权重MAPPO化攻坚令-2026-09-14.md`：§8「最终定义」改写为**三位一体** + 顶部加"口径更新（先读）"横幅（标注阶段3"RL≥规则≥监督"已非部署判据）。
- `docs/验收记录-三元评审权重MAPPO-2026-09-14.md`：新增 §六「三位一体」+ 披露纪律。
- **新定位**：① 解析式最优（capture 100% / 加噪 99.8%，生产默认 `RULE_MODE=analytic`）② RL 兜底解析失效场景（fail-open）③ career 线 RL 真实增益（3-seed 真训 reward 全超规则，唯一有真实增益的 RL 环节）。
- 纪律：任何"MAPPO 调权"表述必须带**场景限定词**；不得写"RL 超一切/优于现状"。
- 备注：README **已含** career 模块说明（第十一节「芒得很职 · 职业素养对抗实训」）与 M3/M5 MAPPO 相关段落，无需新增三位一体专节；后续三创答辩稿须遵循 §8 纪律。

---

## 治理机制落地（CTO §6）

- **§6.1 基线有效性检查门** → 固化为 `tests/test_career_policy_baseline.py`（跑对比前先过 a/b/c 三门）。
- **§6.2 实验产物 meta** → `experiments/train_career_mappo.py` 输出新增 `meta.{baseline_checked, baseline_trigger_rate, seed_reproducible, environment_source}`（缺项按"不可引用"处理）。

---

## 提交与推送

| commit | 内容 |
|---|---|
| `2c27ff5` | 派单一：代码 + 测试 + 3-seed 数据 + 重训 checkpoint（6 文件 +195/−10） |
| `880ff95` | 派单二/三：攻坚令 §8 + 验收记录 + 归档 CTO 派单令（3 文件 +260/−2） |

**远端**：`origin/career-literacy` = `origin/main` = `mars408` = **`880ff95`**（git status 干净）。

*本回执由执行会话产出，所有数字来自实跑与落盘 JSON。*
